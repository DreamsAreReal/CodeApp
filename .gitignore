node_modules/
.env
deploy/.env
.DS_Store
.verify/
.spikes/**/out/
.spikes/**/dist/
**/bin/
**/obj/
**/publish/

# harness re-run noise: PY-lesson evidence PNGs regenerate on every verify run
docs/tasks/python-track/evidence/harness/

# build outputs (rebuilt by CI / the Docker image — never committed)
app/dist/
app/dist-deploy/
backend/Codex.Backend/wwwroot/

# SQLite runtime databases (durable data lives on the server volume, not in git)
*.db
*.db-wal
*.db-shm

# Session transcript export (local only — never committed/pushed)
_session-transcript/

# personal study notes dropped into the repo root are never tracked
/[0-9][0-9]-*.md
