# Copy to deploy/.env (git-ignored) and fill in. NEVER commit the real token.
# Get it from @BotFather (/newbot). Compose reads this file automatically.
TELEGRAM_BOT_TOKEN=123456789:replace-with-your-real-bot-token
