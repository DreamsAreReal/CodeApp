# Вердикт турниров F1 (оркестратор, парные сравнения в обоих порядках, 2026-07-15)

## Нулевой экран (home, 2 трека): ПОБЕДИТЕЛЬ A1 «секции стопкой»
- A1>A2 (оба порядка): A2 прячет неактивный трек — дневная очередь охватывает оба трека,
  скрытие добавляет трение + новый stateful-контрол против LOCKED-минимализма.
- A1>A3 (оба порядка): A3 сворачивает C#-секцию — понижение существующего трека, новая
  интеракция. A1 = обе секции видны, непрерывный скролл, минимальная дельта к home.ts.
- ГРЕФТЫ победителю: сабтайтл Python-секции («6 механизмов, из которых сделаны твои
  инструменты» — из A3), явные лейблы-разделители секций (из A2), бейдж «новый трек» (A1).

## Signature «невидимое»: ПОБЕДИТЕЛЬ B1 «dis-панель + refs-чип»
- B1>B2 (оба порядка): B2 (рентген-слой) — сильная метафора, но одноконцептная (closures);
  B1 обобщается на весь трек (декораторы/генераторы/имена) и переиспользует ПРОВЕРЕННУЮ
  механику IL-панели (ilLine-синхронизация уже в движке) — ноль движкового риска.
- B1>B3 (оба порядка): B3 (живой refcount+шкала) частично уже входит в B1 (refs-чип);
  кнопка Execute из мокапа не в модели движка.
- ГРЕФТЫ победителю: «рентген-зона» с пунктиром для скрытых сущностей (__closure__ в
  уроке замыканий; generic-стиль зоны, если нужен — одна CSS-строка, не per-lesson хак)
  из B2; refs-чип с coral-вспышкой при изменении (accent-флаг) из B3.

Скрины: .spikes/tournament/{home-A1..A3,sig-B1..B3}.png. Мокапы — НЕ деливерабл.
