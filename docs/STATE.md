<!-- Шаблон STATE.md. Оркестратор обновляет ДО спавна агента и после каждого изменения статуса. -->
# STATE — TG Mini App для геймифицированного обучения (C# / CS / LLM)

## Мета
Задача: Джун-разработчик C# хочет Telegram Mini App, куда заходит каждый день и в игровой,
геймифицированной форме изучает и запоминает фундаментальные вещи (C#, БД, очереди, структуры
данных, механизмы языка, принципы построения приложений, конвенции, паттерны, алгоритмы, как
работают LLM/Claude/Codex, тюнинг локальных моделей). Методика — на научной базе эффективного
запоминания (интервальные повторения, активное припоминание и т.п.). Форма — простая, игровая,
с визуализацией и понятными объяснениями, но БЕЗ потери глубины (не в ущерб упрощению). Цель —
дойти до экспертного понимания «как будто я сам всё написал».
Класс: software-UI (Telegram Mini App), интерактивный продукт + крупный research/контентный
подкомпонент (mixed, primary = software-UI interactive). Тип старта: greenfield.
Режим: полный (кандидат — ратифицируется пользователем на гейте; альтернатива — lite при
выборе узкого вертикального среза; тайный триаж запрещён, режим выбирает пользователь)
Workspace: /Users/admin/Desktop/test5/docs/
Обновлено: 2026-07-10

## Флаги
- SAFETY: нет (образовательный контент, без здоровья/права/денег/решений о людях/детей)
- Недоступный канал восприятия: нет (текст + визуал, полностью доступно)
- high-stakes: нет
- Интерактивный продукт: ДА (ценность во времени взаимодействия → секция baselines
  «Глубина контента и пейсинг» применима целиком: таймлайн новизны, sim-harness, «минута смерти»)

## Фазы (словарь = стейт-машина SKILL)
- [x] 0 INTAKE
- [x] 1 RESEARCH (брейншторм + ресерч + review + RS-08..15) — ACCEPT-WITH-NOTES, must-fix закрыты
- [x] 2 ТЗ — Гейт ТЗ: УТВЕРЖДЕНО 2026-07-09 (name=placeholder/без имени; фокус=C#-ядро вглубь; старт=walking skeleton + пара уроков + AI-инструкция + расширяемое приложение) — чекпойнт после скелета: да
- [x] 3 DESIGN (дизайн mid LOCKED + формат lesson-as-data/segments + движок RS-08 + бэкенд построен)
- [ ] 4 BUILD (builder; майлстоуны: 0/~5 [skeleton→loop→lessons→TMA→deploy])
- [ ] 5 VERIFY (финал + ретро)
Статусы завершения: «готова волна N» (предложена следующая волна; НЕ конец) | BETA
(нетерминальный, ждёт решения пользователя) | DONE (пользователь сказал «хватит»)

## Рекап задачи (ЖИВОЙ — обновляется при смене вводных)
Пользователь — джун-разработчик C#. Хочет приложение-Telegram Mini App, чтобы заходить КАЖДЫЙ
ДЕНЬ и в геймифицированной форме изучать и ЗАПОМИНАТЬ разные вещи — «понимать и осознавать».
Основано на научной практике построения эффективных обучающих приложений. Кейс использования:
«в поездке открыть и поизучать всякое».
Что хочет изучить (полный охват, «на атомы разложить»): от самого C#, базы данных, очереди,
типы данных, механизмы работы языка, принципы построения приложений, конвенции и прочее;
паттерны, алгоритмы; как работают LLM, Claude, Codex; как тюнить локальные модели. Просит
«погуглить стек, что юзают, каждый инструмент изучить досконально», плюс принципы разработки.
Цель — «дойти до эксперта, как будто это я сам всё написал», включая алгоритмы и всё, что с
ними связано.
Форма: относительно простая, с визуализацией и понятными объяснениями, но НЕ скрывая деталей.
Максимально охватить базу обучения. Форма должна быть понятная и ИГРОВАЯ — чтобы было ПРАВДА
интересно и, самое главное, ПОНЯТНО, но НЕ в ущерб упрощению. Хочет знать даже какие алгоритмы
и где используются, сравнения и т.д. Итог: «всё изучить и максимально не больно».
Открытые вопросы для гейта (НЕ спрашивать на intake): планка/режим; охват curriculum (весь
сразу vs вертикальный срез); на чём строить сам app (в т.ч. вариант на C#/Blazor, чтобы
пользователь и из кодовой базы учился); нужен ли бэкенд/персистентность интервальных повторений;
язык контента (по умолчанию — русский, как язык пользователя).

## Следующий шаг

### BUILD-статус (ТЕКУЩИЙ — 2026-07-10)
Фаза 4 BUILD идёт. Собрано и ЗЕЛЕНО (npm run build + npm run verify, live backend):
walking skeleton + движок (RS-08) + бэкенд C#/ASP.NET+SQLite+FSRS-6 (12 xUnit PASS) +
AI-инструкция (docs/AUTHORING-AI.md). Уроки-фронт (LessonData): value-vs-reference, boxing,
gc, closures, async-await = 5 уроков, все рендерятся, анимация играет, карточка двигает
FSRS-расписание, 0 ошибок консоли.
- 2026-07-10 — АУДИТ ТОЧНОСТИ (риск №1): в gc/closures/async найдены цитатные дефекты
  (факты верны, но verbatim-цитаты выдуманы/мисатрибутированы). ИСПРАВЛЕНО точечно:
  closures — убрана ВЫДУМАННАЯ цитата ms-lambda «…evaluates the captured variables…» (2 места)
  → переформулировано своими словами; убран cs-history из C#5-claim; якорь спеки → §13.9.5.2.
  async — источник async-void quote «…can't catch any exceptions…» ms-async-kw→ms-tap
  (проверено fetch: verbatim живёт на Task-async-programming-model); «there is no thread»
  атрибутирован Stephen Cleary (не офдок). gc — «allocating»→«allocates» (verbatim источника).
  Пересборка + verify = ВСЁ ЗЕЛЕНО (F1–F5, 0 консоль-ошибок).
- 2026-07-10 — Найдена дыра: seed T2.M5.hashtable.json есть на бэке (карта c1 в FSRS-очереди),
  но НЕТ фронт-урока → карта неоткрываема. ЗАКРЫТА: собран app/src/lessons/hashtable.ts (тот же
  id + card c1), 6 анимированных сегментов (bucket-index → коллизия/цепочка → равенство ключей →
  худший O(n) → resize/rehash → семантика перезаписи), viz = массив bucket'ов (slot/chip/ref/gate,
  без stack/heap). Банк цитат соблюдён: НЕЗАВИСИМЫЙ аудит — все 10 англ. «…»-цитат из банка .NET
  Dictionary Remarks, 0 выдумок. G-EXEC карты c1 (top-level statements) = реальный stdout «2» =
  seed. Пересборка + ОБА verify-харнесса (run.mjs + new-lessons.mjs) = ВСЁ ЗЕЛЕНО, 0 консоль-ошибок.
  Побочно: builder починил пред-существующий flake в new-lessons.mjs (гонка ожидания __viz.ready
  на gc) — ассерты не тронуты; и я задокументировал в AUTHORING-AI.md §9 грабли run-csharp
  (top-level statements, НЕ class/Main → пустой stdout). ИТОГ: 6 уроков-фронт, все зелёные.
  [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-10 — ВЫБОР ПОЛЬЗОВАТЕЛЯ (гейт след.шага): реальный Telegram-деплой. Сделал приложение
  DEPLOY-READY (single-origin): бэкенд теперь раздаёт фронт (wwwroot) + /api с одного HTTPS-хоста.
  Program.cs: UseDefaultFiles/UseStaticFiles/MapFallbackToFile (guarded — нет wwwroot → API-only).
  appsettings.Production.json: DevMode=false (локдаун /api/dev/* + run-csharp), токен из env
  Telegram__BotToken (не коммитим). package.json: build:deploy (VITE_API_BASE= → относительная
  same-origin сборка в dist-deploy, НЕ клобберит dev-dist) + deploy:pack (→ wwwroot). Фикс strings.ts
  errorBody (убрал localhost:5080). ПРОВЕРЕНО ЛОКАЛЬНО без Telegram: [1] GET / отдаёт приложение,
  /api/lessons=6; [2] реальный HMAC round-trip (sign→validate тем же токеном → mode telegram, user
  42/77); [3] подделка initData → 401; [4] Production-локдаун devUserId/sign/run-csharp → 403, но
  настоящий initData → 200. app/dist восстановлен в dev-сборку, оба verify-харнесса ЗЕЛЕНЫЕ.
  Написан DEPLOY.md (путь A: cloudflared quick-tunnel + BotFather ~5 мин; путь B: постоянный хостинг).
  ОСТАЁТСЯ ЗА ПОЛЬЗОВАТЕЛЕМ (не могу — его аккаунт/секрет): создать бота @BotFather, дать токен через
  env, поднять туннель, прописать URL Mini App, открыть с телефона. [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-10 — ХОСТИНГ (вопросы польз.: «куда бесплатно / много проектов», «почему sqlite»): выбор =
  бесплатно + много проектов на одной коробке. Разъяснил: Cloudflare не запускает ASP.NET (Workers=
  JS/WASM), Fly free-tier умер в 2026 (платно, множится на проекты), Render free = эфемерный диск
  (SQLite стирается). Реко = Oracle Always Free ARM-VM + Caddy (одна коробка, много проектов по
  поддоменам), SQLite остаётся (свой диск). Обоснование SQLite: single-writer/личная нагрузка, ноль
  ops, наш RS-12/RS-15 дизайн, легко бэкапить/свапнуть. СПАЙКИ (реальные): dotnet publish -r
  linux-arm64 OK + нативный libe_sqlite3.so под ARM присутствует; опубликованный dll запущен как в
  контейнере (ContentRoot=WORKDIR) → GET / 200, /api/lessons=6, run-csharp 403 (prod), SQLite на
  примонтированном /data, 0 ошибок старта. Захардил .csproj: seed/**/*.json → CopyToPublishDirectory
  (гарантия в publish; иначе краш старта). НАПИСАНЫ АРТЕФАКТЫ: deploy/Dockerfile (multi-stage single-
  origin, multi-arch/ARM), docker-compose.yml (volume codexdata + токен из .env), Caddyfile.example,
  .env.example, oracle-free-setup.md (пошагово: VM→порты→DuckDNS→Docker→Caddy→BotFather→+проекты),
  .dockerignore; DEPLOY.md обновлён. NB: сам образ локально НЕ собирал (Docker на маке нет) — проверен
  .NET-уровень (publish+run+ARM-SQLite+seed); сборка образа — на VM. Предложить: поставить colima и
  реально собрать образ, ИЛИ пользователь собирает на Oracle-VM. [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-10 — У ПОЛЬЗОВАТЕЛЯ УЖЕ ЕСТЬ VPS+Docker, просит «собери чтоб деплоить без моего участия +
  CI/CD на GitHub Actions». СДЕЛАНО: .github/workflows/deploy.yml (push main → test[dotnet test+npm
  build] → build-and-push образ в GHCR через docker/build-push-action → deploy через appleboy/ssh-action:
  self-provisioning docker-compose на VPS, docker login GHCR встроенным GITHUB_TOKEN, pull+up -d, том
  codexdata не трогается). deploy/docker-compose.prod.yml (pull из GHCR), deploy/cicd-github-actions.md
  (секреты VPS_HOST/USER/SSH_KEY/PORT + TELEGRAM_BOT_TOKEN, разовая подготовка VPS+Caddy, первый push).
  Захардил .gitignore (*.db, deploy/.env, dist, wwwroot — codex.db НЕ коммитим). ПРОВЕРЕНО: все YAML
  валидны (ruby), heredoc-терминатор встаёт в col0 (compose пишется корректно), dotnet test 12/12 PASS,
  npm build зелёно, секрет-скан чист (только placeholder-ы). git commit 5c6f3ad (313 файлов, app+backend+
  deploy+CI) на main ЛОКАЛЬНО (remote нет, НЕ пушил). ГРАНИЦА: сам workflow и SSH-деплой на живой VPS
  прогнать не могу (нет доступа) — исполнится при первом пуше; шаги по канонному паттерну. ОСТАЁТСЯ ЗА
  ПОЛЬЗОВАТЕЛЕМ: создать GitHub-репо, git remote add + push, задать 5 секретов, разово поднять Caddy+домен.
  gh НЕ установлен (репо создаёт пользователь). [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-10 — ЗАПУШЕНО + ОБРАЗ ПРОВЕРЕН. GitHub SSH: пользователь добавил СТАРЫЙ vps-ключ (6vBM…),
  а конфиг я настроил на новый (pUpx…) → рассинхрон; переключил ~/.ssh/config github.com → id_ed25519
  (тот, что добавлен). ssh -T = «Hi DreamsAreReal!», git push -u origin main = [new branch] main OK →
  код на github.com/DreamsAreReal/CodeApp, первый CI-прогон пошёл. DOCKER (пользователь включил):
  docker build -f deploy/Dockerfile = OK (497MB, arm64). Запуск контейнера ПОДТВЕРЖДЁН на чистом порту
  (первый замер врал — на хост-порту 8099 висел ПОСТОРОННИЙ python http.server, docker-порт столкнулся
  по IPv6): /health ok, GET / 200, /api/lessons=6, run-csharp 403, sign-initdata 403, SQLite на volume.
  Dockerfile теперь доказан не только на .NET-уровне, но и реальной сборкой+запуском образа. ОСТАЛОСЬ ЗА
  ПОЛЬЗОВАТЕЛЕМ для авто-деплоя: 5 секретов в репо (VPS_HOST/USER/SSH_KEY/PORT + TELEGRAM_BOT_TOKEN) +
  разово Caddy+домен на VPS. Первый CI: test+build-and-push пройдут, deploy упадёт до секретов (ожидаемо).
  [ЭФФЕКТ-ПРИМЕНЁН]

### История DESIGN-фазы (архив)
brief.md доведён по критике R1 (закрыты: БЛОКЕР T4-очереди→P2; G3 усилен census+existence-check+N≥60;
ставка вау → одна ось генерация/реконструкция C#-ядра + M0.1, созвездие→P1; каденс волны 2; хранилище/
band/гейт-вопросы≤3). Critic раунд 2 = ПРИНЯТО-С-ПРАВКАМИ. СРЕЗ выбран = «Ядро C#».
ДИЗАЙН-ИТЕРАЦИЯ: 4 CSS-концепта (concepts/1..4) пользователь ОТВЕРГ — «не по пинтересту»: чистый
вектор-UI, потерял кино-фотографичность/атмосферу борда-3 (NOCTURNE, filmic-фото, RECLARIUM). Урок:
кино = реальная фото-атмосфера/грейд, не только тёмный UI+glow. Развилка «сколько кино» = ГИБРИД.
Пользователь ТКНУЛ в точные референсы: m07/m15/m20/m22 (refs/cinematic/). ДНК = EDITORIAL CINEMA:
огромная смешанная типографика (heavy sans + italic serif), РЕАЛЬНАЯ кино-фотография (motion-blur,
цветной свет, film-stills) как обложки/герой, журнальная вёрстка, ТЁПЛЫЙ грейд (golden/red-noir),
НЕ холодный cyan/violet. Акцент → тёплый (amber/red). Скачал 18 Unsplash-фото (concepts/assets/:
gh01-10 golden-hour, rd01-08 red-drama). Построен концепт concepts/cine/index.html (Editorial Cinema:
постерный CODEX на кино-герое gh01, темы = редакционные обложки с фото gh07/gh04/rd03/rd08, карточка
boxing + hash-виз в тёплом грейде с зерном). Отрендерен headless-chrome, отправлены crops
(s1-home/s2-card/s3-viz/overview) пользователю. Ждём реакцию: это направление (→ lock design) или
твики (больше red-noir m07 / светлее / др. фото). После lock: гейт (планка + Утверждаю) → фаза 3.
NB: браузер-расширение отключилось; рендер идёт через headless chrome (работает без расширения).
- 2026-07-08 — Концепт cine/ (Editorial Cinema, золото) ОТВЕРГНУТ: «цыганский дизайн» (передержал
  в золото/кич) + «приложение не называется codex» (я выдумал имя — НЕЛЬЗЯ). НАЗВАНИЯ ПОКА НЕТ.
  Направление уточнено: пользователь хочет «что-то как m22» = белая элегантная смешанная типографика
  поверх тёмного ПРИГЛУШЁННОГО атмосферного фото (туман/природа), документально-редакционно, БЕЗ
  золота/красного/кича, сдержанно. Скачал 12 misty-фото (concepts/assets-mist/ms01-12). Построен
  концепт concepts/cine-m22/index.html (m22-язык: приглушённый туманный монохром, белая элегантная
  смешанная типографика Geist800+Fraunces-italic, документальный список тем с misty-миниатюрами,
  заглушка «[ НАЗВАНИЕ ]», акцент = только светлый фокус, ноль золота/красного). Отрендерен headless,
  crops отправлены пользователю (evidence/cine-m22/ тоже). Ждём реакцию: это направление (→ lock
  design-язык) или твики. NB: расширение опять отвалилось — рендер только headless.
  ОТКРЫТО: название приложения (нет), планка, «Утверждаю» — после lock дизайна.
- 2026-07-08 — m22-концепт: «да, но ещё не то». Диагноз пользователя: проблема в КОМПОЗИЦИИ,
  АКЦЕНТАХ, иерархии — «не только цвета». Мои макеты слишком безопасные/однородные/app-conventional;
  референсы держатся на экстрем-контрасте масштаба, ломающей строки типографике, асимметрии, воздухе,
  слоистой глубине, точечных РАЗНЫХ акцентах. Грейд СОХРАНЯЕТСЯ (приглушённый m22, без золота).
  → строю 3 РАЗНЫЕ КОМПОЗИЦИИ home-экрана (concepts/comp/{A,B,C}): A=Cover(магазин-обложка),
  B=Spread(type-first editorial), C=Poster(слоистый драматичный). Мобайл 390, misty-грейд.
  Рендер headless → показать → пользователь тычет в композицию → lock → расширить на все экраны.
- 2026-07-08 — 3 композиции построены (comp/A cover, B spread, C poster), отрендерены, compare.jpg
  + frame.png отправлены, 3 вкладки открыты вживую (open). Ждём выбор композиции (или микс деталей).
  Мой read: A самая элегантная/разрешённая; B самый смелый тип (край-bleed надо доводить);
  C самый драматичный/слоистый (masonry тесновата на мобайле). Грейд одобрен, варьируется КОМПОЗИЦИЯ.
- 2026-07-08 — Пользователь: «шрифты и прочее подбирать тоже надо». → typography как отдельная ось.
  Собран шрифтовой специмен concepts/type/index.html — 6 CYRILLIC-SAFE пар (1 Fraunces, 2 Playfair,
  3 Oswald+Newsreader, 4 Unbounded, 5 Cormorant, 6 Onest+Fraunces) на одном заголовке в приглушённом
  грейде. ВАЖНО: первая версия имела Archivo Black/Anton/Bricolage/Instrument БЕЗ кириллицы (fallback-
  ломка) — переделал на кириллические. Отрендерен, отправлен, открыт вживую.
- 2026-07-08 — ТИПОГРАФИКА ВЫБРАНА: #03 = Oswald (condensed, uppercase) дисплей + Newsreader italic
  акцент; тело Onest, код JetBrains Mono. Грейд приглушённый m22. КОМПОЗИЦИЯ ещё не выбрана. Решение:
  собрать настоящий home = композиция A (обложка) + тип #03, показать, дать переставить на B/C.
  Спавню builder → concepts/home-v1/. TASTE-TOKENS (в brief-паспорт после lock дизайна): fonts
  Oswald+Newsreader+Onest+JetBrainsMono; грейд приглушённый туманный монохром; direction m22-editorial;
  название ПОКА НЕТ (placeholder). Открытые гейт-вопросы: название, планка, «Утверждаю».
- 2026-07-08 — HOME v1 собран (concepts/home-v1/index.html): композиция A × шрифт 03, приглушённый
  монохром, condensed-заголовок на туманном кадре, тип-индекс тем, 1 фокус. Отрендерен, frame.png
  отправлен, открыт вживую. Ждём реакцию: lock design-язык (→ расширить на card+viz, потом гейт:
  название/планка/Утверждаю) или твики (layout B/C, детали).

## Синтез research (фаза 1, свод 6 линз + критика ACCEPT-WITH-NOTES)
1. Curriculum: 7 треков (C#/.NET, CS-фундамент, БД, очереди/конкур., дизайн ПО, LLM, тюнинг),
   ~63 модуля, ~1980–2480 карточек, полный охват = 12–20 мес. → срез на гейте. Реко: вариант A
   (вертикаль-ядро C#: T1 M1.1–1.6 + T2 базовые + M0.1 SRS-мета, ~180–230 карточек) волной 1.
2. Движок повторений = FSRS-6 (точнее SM-2 в ~99% коллекций по log-loss; «−20-30% повторов» —
   модельная оценка, НЕ первичка). SM-2/HLR/Leitner = учебные кейсы, НЕ движок.
3. Методика (RS-01): активное припоминание/ГЕНЕРАЦИЯ (не узнавание), таргет ~85% desirable
   difficulty (Wilson 2019), калибровка (Brier), interleaving, spacing, concept-DAG по
   пререквизитам, мета-прозрачность (app показывает свой FSRS = учебный экспонат).
4. Стек (proba исполнена): фронт = статический JS/TS TMA (light, <200KB, client-only
   CloudStorage+IndexedDB, offline для поездки). Blazor WASM = ~2.8MB brotli, ~14× тяжелее,
   22s TTI на 3G → НЕ фронт; C# учим в контенте, бэкенд C# опционален позже.
5. Визуал: НАПРАВЛЕНИЕ 3 «Cinematic App Shell» (выбор пользователя 2026-07-08).
6. Метрики/guardrails: delayed-retention ≥14д, mastered P(L)≥0.95, калибровка, анти-фарм
   детектор (learner-vs-farmer); «не хуже»-guardrails. Риск №1 = ТОЧНОСТЬ контента (каждый
   факт → первоисточник + verification pass). Геймификация здоровая (без streak-shaming).
7. Цена планки (proba): MVP ≈3 майлстоуна, готовый daily-learning ≈8–10.
Maximalist-ставка: «self-hosting learning app» — приложение как вскрываемый/пересобираемый
препарат всего, чему учит (FSRS/очередь/LLM-слой = и урок, и его код). Ось «как будто я сам написал».

## Фиче-борд (зеркало docs/features.md — там источник правды)
| F | Название | Майлстоун | Золотой путь | Статус | Раунд оценки |
|---|---|---|---|---|---|

## Карта рисков / сомнения агентов
- Риск: огромный охват curriculum vs «понятно и не больно» → конфликт широты и глубины.
  Разрешается выбором режима/среза на гейте + научной методикой дозирования.
- Риск: «без потери глубины» + «игровая простая форма» — двойное ограничение, требует
  сильного паспорта вкуса и деконструкции эталонов в фазе 1.

## Backlog (МЕЛОЧИ / P2-хвосты)
- <пункт из «ЧТО ПЛОХО» или P2>

## Журнал (append-only, не редактировать прошлые строки)
- 2026-07-08 — INTAKE: классифицирован software-UI/interactive + research-подкомпонент,
  greenfield, флаги выставлены (все нет), workspace docs/ создан, git init выполнен.
- 2026-07-08 — PHASE 1: написан RS-00-brainstorm.md (13 углов). Запущен research-workflow
  wio8loc55 (background): 2 critic-брейнштормера + 6 researcher-ов (методика/TMA-стек/эталоны/
  curriculum/метрики-риски/визуализация) → барьер → probe + 3 стилевые заглушки + research-critic.
  Ждём завершения для синтеза и подготовки к гейту ТЗ.
- 2026-07-08 — DESIGN PROBE: создана стилевая заглушка #2 «Bright playful STEM»
  (docs/research/stubs/2/index.html, 16KB): hero с градиентом, карточка урока async/await,
  визуализация flow, gameification (streak/XP), мудборд-блок (палитра 5 цветов + типография +
  tone-guide). Мобильный вьюпорт 390px, inline CSS, готова для дизайн-гейта.
- 2026-07-08 — ОТВЕРГНУТ дизайн-slop: пользователь отверг haiku-заглушки как «нейрослоп, нет
  вау», планка → «мега красиво / как кино». Записана feedback-память
  design-taste-cinematic-no-aislop. Заглушки stubs/* НЕ использовать как визуальную основу.
- 2026-07-08 — REFS: вошёл в живой Pinterest (расширение), деконструированы 3 направления,
  скачано 78 реальных изображений в docs/research/refs/{constellation,fui,cinematic}/, собраны
  3 борда refs/boards/board-1..3.jpg (проверены глазами через Read). Написан
  RS-07-artdirection.md. Борды отправлены пользователю, ждём выбор визуального направления.
- 2026-07-08 — БОЛЬШОЙ РАЗВОРОТ: пользователь «вообще давай лучше ближе аля дуолинго». Отказ от
  киношно-тёмного/editorial (m22) в пользу Duolingo-стиля: светло, ярко, дружелюбно, скруглённый
  шрифт, геймификация-путь, стрик/XP/сердечки, 3D-кнопки, маскот. Противоречит feedback-памяти
  design-taste-cinematic-no-aislop (НЕ трогаю память до стабилизации — направление в потоке). База:
  RS-03 §2.2 деконструкция Duolingo (палитра Feather Green #58CC02 и др., Feather Bold rounded, 3D-
  кнопка, lesson path, streak/XP/лиги). Строю concepts/duo/ home: путь тем C#-ядра + топ-бар
  геймификации + скруглённый Nunito (кириллица) + яркая палитра + маскот-заглушка. home-v1 (кино)
  сохранён как альтернатива. Название всё ещё placeholder.
- 2026-07-08 — Duolingo-style home собран (concepts/duo/index.html): светлый, Feather-green,
  Nunito rounded, 3D-кнопки, путь уроков C#-ядра (value-vs-ref✓/стек-куча✓/boxing-СТАРТ/замки),
  топ-бар стрик/гемы/сердца, маскот-плейсхолдер, таб-бар. Отрендерен, frame отправлен, открыт вживую.
  Ждём реакцию: это направление (→ lock, расширить на экран урока, потом гейт) или назад к кино
  (home-v1)/микс. NB: 2 живых направления — кино (home-v1) и Duolingo (duo). Название всё ещё нет.
- 2026-07-08 — Пользователь: «я бы сделал СРЕДНЕЕ, а не копирку». → оригинальный middle между кино и
  Duolingo: тёплый/дружелюбный/геймифицированный, но со своим вкусом, НЕ клон (не зелёный/сова, не
  мрачное кино). Моя дизайн-ставка (беру решение на себя): тёплая КРЕМОВАЯ база (#F6F1E9), акцент
  КОРАЛЛ (#F0533A) + вторичный soft, шрифт Rubik (скруглённо-взрослый, кириллица) + Onest/JetBrains
  Mono, мягкая глубина вместо кислотных 3D, аккуратная геймификация (прогресс/стрик/XP без детсада),
  минимальный СВОЙ характер вместо совы. Строю concepts/mid/ home C#-ядра. Название = placeholder.
- 2026-07-08 — MIDDLE home собран (concepts/mid/index.html): крем #F6F1E9 + коралл #F0533A, Rubik+
  Onest, hero-карта «Продолжить сегодня» (кольцо 60%, мягкая 3D-кнопка), путь тем карточками с line-
  иконами (Типы/Асинхр/Коллекции-lock/GC), тихие метрики-чипы, «спарк»-знак вместо совы, нижняя нав.
  Своё лицо: НЕ Duolingo-клон, НЕ кино-мрак. Отрендерен, frame отправлен, открыт вживую. Ждём реакцию
  → при lock: расширить на экран урока + гейт (название/планка/Утверждаю). Тек. кандидаты дизайна:
  mid (лидер) | duo | home-v1(кино). Название всё ещё placeholder.
- 2026-07-08 — ДИЗАЙН-ЯЗЫК ЗАФИКСИРОВАН (пользователь «ооо это круто»): concepts/mid/ = крем #F6F1E9
  + коралл #F0533A + Rubik/Onest + геймификация-со-вкусом + «спарк»-знак. Обновлены памяти
  design-taste-original-warm-middle + design-locked-cream-coral-rubik. Раскатываю язык на экран УРОКА
  (mcq boxing + фидбек+XP) и ВИЗУАЛИЗАЦИЮ (хеш-таблица) → concepts/screens/{lesson,viz}. Параллельно
  предложу НАЗВАНИЯ. После: обновить brief паспорт вкуса под mid + гейт (название/планка/Утверждаю).
- 2026-07-08 — Пользователь вернул к СУТИ: «продумал как делать анимации в уроках? почекал
  исследования как делать такие апки / методический материал? должно ОЧЕНЬ глубоко все нюансы
  объяснять — хочу разбираться на уровне ЭКСПЕРТА». Честно: RS-01(методика)/RS-06(визуализация)/
  RS-04(curriculum) есть, но НЕ докопаны (a) тех-архитектура построения интерактивных анимаций и
  (b) инструкционный дизайн для экспертной глубины + контент-пайплайн. → ГЛУБОКИЙ ресёрч (deferred:
  экраны lesson/viz — после). Запущен workflow: RS-08 анимация-архитектура, RS-09 instructional-design-
  for-depth, RS-10 контент-пайплайн+lesson-as-data-schema, RS-11 деконструкция эталонов глубоких
  объяснений (Python Tutor/Brilliant/3B1B/execute-program). Синтез → конкретный ответ + в brief.
- 2026-07-08 — Глубокий ресёрч ГОТОВ (RS-08 аним-архитектура [spike .spikes/rs08-living-diagram PASS
  7/7, движок 1.28KB gz], RS-09 7-фазный шаблон урока, RS-10 lesson-as-data + детерм. гейты точности
  исполнены, RS-11 эталоны). Дан пользователю синтез-ответ. Пользователь: «ну концепты покажи». →
  строю ИНТЕРАКТИВНЫЙ урок boxing (concepts/lesson-boxing/): рабочая SVG-анимация стек/куча на
  living-diagram движке + predict-then-run + 7-фазная глубина, в дизайне mid (крем+коралл+Rubik).
  Показать вживую. Потом: свернуть методику/схему/движок в brief-паспорт + гейт.
- 2026-07-08 — ФЛАГМАН-КОНЦЕПТ урока boxing собран (concepts/lesson-boxing/index.html), 40/40 headless-
  assert green: рабочий living-diagram движок (Scene→SVG→data-join→FLIP→скраббер), predict-then-run
  гейт, REF-стрелка, MCQ+XP, edge-case GC, источник MS Learn (сверено). 5 секций = 7-фазная глубина.
  story.jpg + boxing-кадр отправлены, открыт вживую. Демонстрирует анимацию+глубину+дизайн mid в деле.
  Ждём реакцию → далее: свернуть методику(RS-09)/схему(RS-10)/движок(RS-08) в brief-паспорт + гейт
  (название/планка/Утверждаю). Дизайн mid + этот урок = основа сборки.
- 2026-07-08 — Пользователь: «концепт ахуенный; нужен ЕДИНЫЙ ФОРМАТ занятий (удобно добавлять с
  разными анимациями/виз) + не хватает ГЛУБИНЫ (стек 1МБ/поток, у каждого потока свой стек)».
  → (1) Написан docs/design/lesson-format.md — единый формат урок-как-данные (из RS-10 §5/§6 +
  каталог виз-примитивов RS-08): один объект lesson драйвит анимацию+SRS+текст, добавить урок =
  заполнить объект. (2) Спавнен builder: сделать lesson-boxing DATA-DRIVEN (виден объект lesson) +
  углубить (раскрываемый экспертный слой: свой стек у потока, размер по умолч. сверить с MS Learn,
  где живут value-типы, stack overflow, цена boxing) + 2-я виз-сцена «потоки и стеки» на тех же
  примитивах. Точность = риск №1, сверять дословно. → показать формат + углублённый урок.
- 2026-07-08 — СМЕНА ВВОДНЫХ: пользователь = СЕНЬОР C# (не джун). Цель апки — «снова пройтись по БАЗЕ
  с УГЛУБЛЕНИЯМИ» (рефреш фундамента + нюансы/машинный уровень/gotchas), НЕ обучение с нуля. Тон под
  сеньора. + Глубина ДОЛЖНА быть ВПЛЕТЕНА в урок+анимации, а НЕ отдельным аккордеоном. (Recap-Мета
  выше всё ещё пишет «джун» — читать джун→сеньор; фолдить в brief на гейте.) Дизайн mid / формат
  lesson-as-data / deep-research — держатся (для сеньора даже лучше: expertise-reversal, глубина).
  → перезапуск сборки: интегрировать deepDive-claims + viz2(потоки/стеки) в основной анимационный
  поток урока (убрать аккордеон), калибровать под сеньора. Точность/источники/дизайн/скролл сохранить.
- 2026-07-08 — Урок ПЕРЕСОБРАН: глубина ВПЛЕТЕНА в поток (аккордеон удалён), 22/22 headless PASS.
  Диаграмма памяти несёт подписи «СТЕК ПОТОКА ~1МБ Windows»/«GC-КУЧА общая», 5 нюансов inline с
  дословными MS-Learn цитатами+источниками; сцена «потоки/стеки» = видимый шаг (тот же движок/
  примитивы). Тон под сеньора. data-driven формат сохранён (window.__lesson: viz.zoneNotes/depth/
  threads.viz2/nuance.edges). crop-memory+crop-threads отправлены, открыт вживую. Ждём реакцию.
  СОСТОЯНИЕ: дизайн mid LOCKED · формат lesson-as-data · движок RS-08 · deep-research done ·
  аудитория=сеньор(рефреш+глубина). Следующий большой шаг: свернуть всё в brief (обновить аудиторию/
  паспорт/методику/формат) + ГЕЙТ (название/планка/охват/Утверждаю) → полная сборка.
- 2026-07-09 — «где анимашки»: сделаны GIF реального прогона (anim-memory/anim-threads) — анимация
  работает, просто стартует по play; в проде будет автозапуск-при-появлении. Пользователь «так а
  покажешь?» → строю ВТОРОЙ урок другого ТИПА виз: concepts/lesson-async/ (async/await, таймлайн/
  state-machine — не боксы), тот же движок/формат/дизайн mid, сеньор-тон, глубина вплетена
  (await≠поток, state machine/continuation, captured context/ConfigureAwait, deadlock на .Result,
  Task≠Thread) — сверять с MS Learn (риск №1). + автозапуск анимации по IntersectionObserver
  (respect reduced-motion). Демонстрирует «единый формат тянет разные анимации».
- 2026-07-09 — Урок async/await собран (concepts/lesson-async/, 27/27 headless, 0 ошибок): ДРУГОЙ тип
  виз (таймлайн/стейт-машина, 3 track'а) на ТОМ ЖЕ движке/формате/дизайне + автозапуск (Intersection
  Observer, respect reduced-motion) + глубина сеньора со сверкой MS Learn (await≠поток «extra threads»,
  стейт-машина, captured context/ConfigureAwait, deadlock .Result, Task≠Thread). GIF anim-async снят+
  отправлен. ИТОГ ВАЛИДАЦИИ: 2 типа анимации на одном формате/движке/дизайне → подход подтверждён.
  РЕКОМЕНДАЦИЯ: свести в brief (аудитория-сеньор/паспорт mid/методика RS-09/формат/движок/автозапуск)
  → ГЕЙТ (название/охват волны1/планка/Утверждаю) → полная сборка. Кандидаты дизайна: mid LOCKED.
- 2026-07-09 — Пользователь «покажи» → строю ТРЕТИЙ тип виз: concepts/lesson-attention/ (self-attention
  в LLM). Виз = токены + веса внимания (heatmap/толщина рёбер + softmax-бары), канонич. пример «it→animal».
  Тот же движок/формат/дизайн mid + автозапуск. Шаги: токены→query→Q·K scores→softmax→взвеш.сумма;
  нюансы: scaled dot-product ÷√d_k, multi-head, causal mask. Сверка по arXiv:1706.03762 (риск №1).
  Демонстрирует 3-й тип анимации на едином формате. Затем — свести в brief + гейт (название/охват/планка).
- 2026-07-09 — НОВАЯ ПЛАНКА ГЛУБИНЫ (пользователь: «мало анимаций и углублений; хочу быть ЭКСПЕРТОМ;
  не только начальная инфа — подробно, с объяснениями и АНИМАЦИЯМИ, а не просто упоминать»). Дефект:
  урок = 1 анимация + нюансы «упомянуты» списком. Фикс: КАЖДЫЙ нюанс = своя анимация + полное
  объяснение механизма; МНОГО анимаций/урок. → пересобираю lesson-boxing в ЭКСПЕРТНЫЙ (6-7 анимир.
  разборов: механика/копия≠ссылка/unbox+typecheck+InvalidCast/IL box-unbox.any/где живёт/цена-в-цикле-
  GC-Gen0/generics+скрытый boxing), каждый объяснён+источник+автозапуск. Это новый ЭТАЛОН плотности
  для всех уроков (обновить lesson-format: депт = N анимир. сегментов, не 1 аним+буллеты). Точность=риск№1.
  (attention-урок доедет отдельно; применить ту же плотность.)
- 2026-07-09 — ЭКСПЕРТНЫЙ boxing готов (17/17 headless): 7 анимир. разборов (data: lesson.segments[]),
  каждый = анимация + «Механизм как и почему» + первоисточник; НОВЫЕ примитивы gate(typecheck)+IL-панель
  (box 0x8C/unbox.any 0xA5). Сняты 4 GIF (mechanics/unboxing/gc/generics) + сториборд плотности,
  отправлены. Это НОВЫЙ ЭТАЛОН ПЛОТНОСТИ. Ждём подтверждения бара → codify в lesson-format (депт =
  N анимир. сегментов) + применить к async/attention. NB: такая плотность = урок объёмный → учесть в
  scope/планке волны 1 на гейте.
- 2026-07-09 — Показан attention-урок (anim-attention GIF + кадр «it→animal 52%»). ИТОГ КОНЦЕПТОВ:
  3 типа анимации на ЕДИНОМ формате/движке/дизайне mid — боксы(память/boxing, ЭКСПЕРТ-плотность 7
  сегм.), таймлайн(async), веса(attention). boxing = эталон плотности; async/attention пока старая
  плотность. Всё с автозапуском, сверкой первоисточников, data-driven. ГОТОВО К КОНСОЛИДАЦИИ: обновить
  brief (аудитория-сеньор, паспорт mid, методика RS-09, формат+плотность, движок) → ГЕЙТ (название/
  охват[вглубь vs вширь]/планка) → полная сборка. Пользователь ведёт через «покажи» — показываю по одному.
- 2026-07-09 — Пользователь «очень здорово, проведи ревью и ресёрч того что осталось». → workflow:
  REVIEW (critic, адверсариально всё сделанное: дизайн mid/формат-движок/3 урока-эксперт-плотность/
  методика-под-сеньора/wow-ступень/риски/реалистичность для соло) + 4 РЕСЁРЧА оставшихся дыр:
  RS-12 data/backend&sync (persist FSRS+progress, client-only vs backend, TMA auth/offline, для соло/
  долгосрок сеньора), RS-13 контент-производство в масштабе+стоимость (эксперт-плотность=7 анимир.
  сегм/урок → реальная цена/урок, пайплайн авторинга+верификации, сколько уроков в волну1), RS-14 SRS
  для генеративных/экспертных айтемов + expertise-reversal планирование + автогрейд открытых ответов,
  RS-15 TMA-продакшн-архитектура (тяжёлые интерактив-уроки, offline/PWA в WebView, initData+лёгкий
  бэкенд, deploy/CI/бот, перф-бюджет). Синтез → Ревью+«что осталось» → финал brief + гейт.
- 2026-07-09 — РЕШЕНИЕ ПО АРХИТЕКТУРЕ: пользователь «сделай лучше бэк какой-нить» → БЭКЕНД (не
  client-only). Стек = C#/ASP.NET Core (сеньор-C#, бэк = учебный артефакт, + server-side C#-исполнение
  разблокирует ставку «как будто я сам написал»: write-missing на СКРЫТЫХ ТЕСТАХ исполняется на сервере,
  не нужен браузерный C#-раннер — закрывает Блокер №2 ревью). Строю рабочий скелет backend/: minimal API,
  initData-валидация (HMAC), SQLite-персистентность, FSRS-планировщик, эндпойнты (due/review/grade-code/
  lessons), server-side compile+run C# (Roslyn) для проверки код-ответов. ДОЛЖЕН реально запускаться
  (dotnet run + curl = доказательство). Грунт: RS-12(данные)/RS-02(initData)/RS-14(грейд)/lesson-format.
- 2026-07-09 — УТОЧНЕНИЕ ставки вау (пользователь): «как будто я сам написал» = про ТЕХНОЛОГИИ —
  разбираться в них до внутренностей/IL/машинного уровня, будто сам спроектировал; ЭТО ПРО ГЛУБИНУ
  УРОКОВ, НЕ про написание кода в аппе и НЕ про «self-hosting/reconstruct app». Отменяет прежнюю
  трактовку ставки (write-missing на скрытых тестах / server-side exec как НОСИТЕЛЬ ставки — НЕ нужен).
  → Ставка вау = ГЛУБИНА объяснений (как boxing: IL/память/GC), гнать все уроки на эту глубину.
  КОРРЕКЦИЯ бэкенда: его роль — durable-персистентность (FSRS+прогресс, cross-device, фикс iOS-storage
  RS-12) + раздача контента + initData-auth; server-side C#-исполнение остаётся как инструмент
  АВТОРИНГА (точность ответа = реальный dotnet-вывод), НЕ как user-facing проверка «докажи владение».
  Блокер №2 ревью снимается переопределением ставки (не нужен носитель-исполнитель).
- 2026-07-09 — ГЕЙТ УТВЕРЖДЁН. Решения: без названия (placeholder); фокус волны 1 = C#-ядро ВГЛУБЬ;
  старт = walking skeleton + пара уроков + AI-инструкция + РАСШИРЯЕМОЕ приложение (добавлять уроки потом).
  → ФАЗА 4 BUILD. Milestone 1 (скелет): app/ = Vite+TS расширяемый: engine-модуль (из lesson-boxing) +
  shell/home (mid) + generic lesson-runner (рендерит любой lesson-as-data) + api-клиент (auth/due/review) +
  Telegram-интеграция (initData+тема+haptics, dev-fallback) + уроки как ДАННЫЕ (add файл = add урок).
  Живой FSRS-loop через бэк. + пишу docs/AUTHORING-AI.md (playbook для др. ИИ: как добавить урок).
  Чекпойнт после скелета.
- 2026-07-09 — MILESTONE 1 (walking skeleton) ГОТОВ и НЕЗАВИСИМО ПРОВЕРЕН оркестратором (мой прогон
  npm run verify против живого бэка = ALL GREEN): home+dev-auth+live due (F1); boxing 7 сегм. autoplay
  (F2); карточка→Good→/api/review интервал 3.26д due 4→3 карточка ушла home 3→2 xp=10 (F3, FSRS-петля
  реально работает); value-vs-reference 4 сегм. (F4); reduced-motion + 0 ошибок консоли; JS 22.5КБ gz.
  app/ Vite+TS (engine-модуль + generic LessonRunner + api + telegram+dev-fallback + 2 урока-данные),
  backend C#/ASP.NET+SQLite+FSRS. Расширяемо (add файл=add урок, доказано). AUTHORING-AI.md написан.
  Открыт вживую (localhost:4173). ЧЕКПОЙНТ ПОСЛЕ СКЕЛЕТА. Код НЕ закоммичен (по слову пользователя).
  Дальше: добавлять уроки C#-ядра (playbook готов) / реальный Telegram+deploy / коммит.
- 2026-07-09 — «продолжи» → ФАЗА 4 продолжается: добавляю C#-ядро уроки (наполнение волны 1, цель
  ~10-12). Один builder (деливерабл пишет только builder; уроки шарят registry/seed → последовательно,
  не параллельно). Добавить: GC/управление памятью (Gen0/1/2, mark-sweep, LOH, финализация), замыкания
  (захват переменной цикла — senior-gotcha, closure-класс в IL), async/await (портировать концепт
  lesson-async в app-формат). Каждый: data-файл src/lessons/ + backend seed + registry, точность
  сверить с MS Learn (риск №1), плотность (несколько анимир. сегментов), интегрировать в FSRS-петлю,
  прогнать verify. Playbook — docs/AUTHORING-AI.md.
- 2026-07-09 — Builder оборвался (connection closed) после 1 из 3 уроков: GC добавлен (app/src/lessons/
  gc.ts + seed T1.M4.gc.json + registry [valueVsReference,boxing,gc]), build ОК (JS 27.25КБ gz). НЕ
  добавлены closures, async-await. → доделываю их (2-й builder). Пользователь «ааа/продолжи» (нетерпение).
- 2026-07-10 — 2-й builder тоже оборвался (ConnectionRefused) НО добавил оба урока: closures.ts +
  async-await.ts (+ seed T2.M1/T2.M2 + registry [valueVsReference,boxing,gc,closures,asyncAwait]).
  build ОК (JS 36.24КБ gz). НЕЗАВИСИМО ПРОВЕРЕНО оркестратором: npm run verify F1-F5 ALL GREEN; смоук —
  все 5 уроков рендерят SVG-анимации; петля FSRS работает. МЕЛОЧЬ: 1 консольный 404 (ресурс) — проверить.
  РИСК: gc/closures/async НЕ прошли accuracy-аудит (builder умер до verify фактов) → запускаю audit-
  workflow (3 верификатора против MS Learn). После — фиксы + продолжать наполнение (цель ~10-12).
