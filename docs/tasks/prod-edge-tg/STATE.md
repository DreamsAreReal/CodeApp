# STATE — Прод-вход для Telegram Mini App

## Мета
Задача: Приложение CodeApp задеплоено и живо на VPS 192.3.94.42 (контейнер codex, 127.0.0.1:8091, healthy), но публичного входа нет. Нужно: домен с A-записью на VPS, HTTPS-терминация (на сервере системный nginx с дефолтной заглушкой на :80, :443 закрыт), проксирование на 127.0.0.1:8091 и привязка URL как Telegram Mini App к боту (Telegram требует настоящий HTTPS-домен). Root-SSH есть (alias «vpn»).
Класс: software-service/бот (инфраструктурная подзадача, improvement)
Режим: lite (КАНДИДАТ, спекулятивное ведение; ратификация пользователем на гейте — гейт ещё не открыт; микро не предлагается: есть внешние действия на прод-сервере)
Workspace: /Users/admin/Desktop/CodeApp/docs/tasks/prod-edge-tg
Обновлено: 2026-07-17T17:05:00Z

## Фазы (словарь = стейт-машина SKILL)
- [x] 0 INTAKE
- [ ] 1 RESEARCH (брейншторм + ресерч)
- [ ] 2 ТЗ — Гейт ТЗ: НЕ УТВЕРЖДЕНО — чекпойнт после скелета: нет
- [ ] 3 DESIGN (дизайн + фиче-лист)
- [ ] 4 BUILD (builder; счётчик майлстоунов: 0/?)
- [ ] 5 VERIFY (финал + ретро)

## Флаги
SAFETY: нет. Недоступный канал: нет. High-stakes: НЕТ (изменения обратимы: бэкап nginx-конфига, откат A-записи; денег нет), но прод-сервер ОБЩИЙ — на нём живут другие сервисы (xray-server/xray-sub на :8080/:8443/:2096, zhaba, portfolio-new, concert-vote, deploy-hub-demo) — их трогать нельзя.
Внешние действия: правка прод-nginx, выпуск сертификата, привязка URL к TG-боту (setChatMenuButton) — требуют преавторизации в «Полномочиях» brief.md.

## Рекап задачи (ЖИВОЙ)
Цель: пользователь открывает бота в Telegram → кнопка меню открывает Mini App → приложение реально работает по HTTPS-домену. Ограничения: не сломать соседние сервисы на VPS; nginx уже стоит — предпочтительно использовать его, а не ставить Caddy; порт 8080 хоста занят (xray-sub), приложение на 127.0.0.1:8091; деплой-хаб при редеплое сохраняет порт 8091. Открытый вопрос: какой домен (есть ли у пользователя свой; на VPS есть xray с TLS на 8443 — возможно, домен уже существует). Бот-токен лежит в env контейнера codex на VPS.

## Следующий шаг
Critic ревьюит brief.md (спавн выполнен) → правки ≤3 раундов → гейт пользователю (домен + режим lite + полномочия).

## Синтез research
1. Доменов/сертов на VPS нет (letsencrypt/certbot/caddy отсутствуют) — домен с нуля.
2. 443 свободен изнутри; снаружи закрыт ufw (80 проходит → провайдер не фильтрует): нужен `ufw allow 443/tcp`.
3. Порт 8091 стабилен: зашит в /opt/codex/docker-compose.yml + app.conf deploy-hub; редеплой не меняет.
4. Бот @coseappbot, long polling; кнопка меню web_app «Учить C#» → trycloudflare URL, МЁРТВ с 17.07 16:06 UTC (quick tunnel сменил URL; есть codex-tunnel.service с живым новым URL). Живое подтверждение North Star-проблемы.
5. TG: HTTPS + валидный CA-серт обязательны, IP нельзя; LE одобрен; setChatMenuButton достаточно (BotFather не нужен для кнопки меню); WebView → анти-frame-заголовки не мешают.
6. LE-серт 90 дней → автопродление обязательно, проверка `certbot renew --dry-run`.
7. СПОРНОЕ (RS-02): «DuckDNS только DNS-01» — противоречит deploy/oracle-free-setup.md (DuckDNS+HTTP-01 через Caddy); HTTP-01 требует лишь A-запись. Решение: HTTP-01 по умолчанию, перепроверить фактом при выпуске серта.
8. nginx общий (VPN-инструкция + 3 прокси-приложения) — только отдельный server-блок, бэкап + nginx -t.

## Фиче-борд (зеркало features.md)
| F | Название | Майлстоун | Золотой путь | Статус | Раунд оценки |
|---|---|---|---|---|---|

## Карта рисков / сомнения агентов
- Общий прод-сервер: любое изменение nginx/портов может задеть соседние сервисы → бэкап конфига до правки, `nginx -t` до reload.
- Порт 443 закрыт: возможно, занят/зарезервирован xray (8443 — его TLS) — проверить, свободен ли 443 и firewall.

## Backlog (МЕЛОЧИ / P2-хвосты)

## Журнал (append-only)
- 2026-07-17T17:00:00Z — INTAKE: класс software-service (infra improvement), режим-кандидат lite, workspace docs/tasks/prod-edge-tg создан.
- 2026-07-17T17:02:00Z — Фаза 1: RS-00 брейншторм записан; запущены 2 researcher-а в фоне (RS-01 VPS recon read-only, RS-02 Telegram reqs).
- 2026-07-17T17:05:00Z — Lint-фикс: строка «Режим:» добавлена в Мета; порядок до гейта уточнён (critic-ревью ТЗ обязательно, reviews/ будет заполнен до гейта).
