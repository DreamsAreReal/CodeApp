<!-- Шаблон STATE.md. Оркестратор обновляет ДО спавна агента и после каждого изменения статуса. -->
# STATE — Python-трек: уроки из 00-python-refresher.md

## Мета
Задача: В готовое приложение (TG Mini App, движок living-diagram + lesson-as-data + FSRS)
добавить НОВЫЙ трек «Python»: уроки на основе md-файла `00-python-refresher.md` (2293 строки,
«Блок 0/11 — Python-фундамент для AQA», 15 разделов, из них 6 глубоких механизмов: декораторы,
генераторы/yield, context managers, классы/OOP, type hints, async/await). Чего в md не хватает —
дополнить ресерчем (первоисточники: docs.python.org и т.п.). Следовать гайдам приложения:
docs/GUIDE.md §3 + docs/AUTHORING-AI.md (планка плотности: каждый нюанс = анимированный разбор,
размещение только через `at`, карточки predict-output с реальным stdout). После сборки — деплой
(push в main → CI → VPS, авто).
Класс: контентная волна software-UI продукта (lesson-as-data: research/контент + код-данные),
режим improvement (продукт готов, ступень 3; движок/дизайн/формат LOCKED — не трогаем)
Workspace: /Users/admin/Desktop/CodeApp/docs/tasks/python-track/ (мини-пайп задачи;
продуктовый слой docs/ — общий, НЕ трогаем его STATE)
Режим: полный (ратифицирован пользователем на гейте 2026-07-15)
Обновлено: 2026-07-15

## Флаги
- SAFETY: нет (образовательный контент)
- Недоступный канал восприятия: нет
- high-stakes: нет
- Интерактивный продукт: ДА (петля уже построена; для контентной волны — правила плотности
  и точности из AUTHORING-AI.md; риск №1 = ТОЧНОСТЬ контента, как в основном пайпе)

## Фазы (словарь = стейт-машина SKILL)
- [x] 0 INTAKE
- [x] 1 RESEARCH (RS-00/00b/00c, RS-01+дополнение р.2, RS-02, RS-03, RS-baseline, Explore;
      критика: R-research ДОРАБОТАТЬ → закрыто)
- [x] 2 ТЗ — brief.md ПРИНЯТ critic-ом (R-brief-r3) — Гейт ТЗ: УТВЕРЖДЕНО 2026-07-15
      («утверждаю делай все включительно даже p2»; режим: полный; 11 уроков + P2-шпаргалки;
      треки не пересекаются; собес-флоу+сеньор; смежности md) — чекпойнт после скелета: ДА
      (1-й урок + мультитрек-UI, там же варианты лейбла трека)
- [x] 3 DESIGN (design.md + ADR-0001/0002/0003 + features.md 15 фичей M1–M5; критика
      R-design ДОРАБОТАТЬ→R-design-r2 ПРИНЯТО)
- [ ] 4 BUILD (builder: фича → зелёное → коммит; майлстоуны M1–M5, счётчик 0/5)
- [x] 5 VERIFY (E1/E2/E3 ПРИНЯТО + 2 consumer-а прошли; фикс-такты b3c50a4/6e1406c/bcdaba4;
      деплой доказан: CI 29548427723 SUCCESS, прод-бандл == локальная прод-сборка байт-в-байт)
Статусы завершения: DONE-WAVE-N | BETA | DONE
СТАТУС: DONE-WAVE-1 (предложена волна 2: дозатор new/день · pytest-блок · STAR/открытые
вопросы · dis-кадры M6/M7/M9 · P2-хвосты бэка)

## Рекап задачи (ЖИВОЙ — обновляется при смене вводных)
Пользователь: «я крч в папку кинул python refresh. сделай плиз раздел новый с питоном, сделай
уроки на основании md файла. если чет там не учтено или нужно дополнить поресерчи. затем задеплой
приложение. с анимациями и проч. там есть гайд в приложении как добавлять уроки».
Разбор: (1) новый раздел/трек Python в существующем приложении; (2) уроки на основе
00-python-refresher.md; (3) пробелы дополнить ресерчем; (4) анимации и прочее — по планке
приложения (гайды GUIDE.md/AUTHORING-AI.md); (5) в конце — деплой (CI автодеплоит push в main).
УТОЧНЕНИЯ ГЕЙТА (2026-07-15): «полные уроки по питону» → волна = 11 уроков (P0+P1);
«C# и питон разные разделы, пусть НЕ пересекаются» → мостики C#↔Python ИСКЛЮЧЕНЫ из
Python-уроков, home делит треки строго врозь; «сохрани собес-флоу из md + привнеси
сеньорский взгляд» → собес-блоки = фича (misconceptions/карточки), каждый ответ обоснован
механизмом; «по md пройдись — там смежные уроки и разделы» → хребет §0 и связки
«механизм → где встречаешь в инструментах (pytest/allure/httpx/locust)» сохраняются в
уроках + вводный сегмент-карта в первом уроке.
Контекст продукта: аудитория md-файла — AQA/подготовка к собесам (акцент «на собесе спросят»),
аудитория приложения — сеньор-разработчик, «пройтись по базе глубоко». Файл — блок 0 из 11
(остальные блоки: pytest, Allure, API, UI, Load, Messaging, SQL, CI/CD, Infra, теория, легенда) —
в задаче только Python (блок 0).
Технические якоря: урок = app/src/lessons/<slug>.ts (LessonData, плоская схема сегмента) +
реестр index.ts + seed backend/Codex.Backend/seed/lessons/<id>.json; id урока/карточек фронт==сид;
размещение виз-узлов ТОЛЬКО at:{zone,row,col}/{in}; дизайн-токены mid (cream/coral/Rubik) LOCKED;
карточки predict-output: verify.expect = РЕАЛЬНЫЙ stdout — для Python считать локальным python3
(бэковый /api/authoring/run-csharp — только для C#); track id для Python — выбрать в DESIGN
(на бэке треки = общие ID T1/T2/…). Харнессы: run/shell/new-lessons/viz-fit/loop + dotnet test.
Деплой: git push main → GH Actions → GHCR → VPS (sha-pinned).

## Следующий шаг
M1 ЗАКРЫТ: монитор чист, evaluator ПРИНЯТО (F1 verified), consumer прошёл с ценностью,
ЧЕКПОЙНТ ПОЛЬЗОВАТЕЛЯ ПРОЙДЕН («прикольно») с 2 решениями: (1) home = ПЕРЕКЛЮЧАТЕЛЬ
ТРЕКОВ вместо секций стопкой (масштаб на N треков); (2) лейбл = «Python для AQA».
M2 ЗАКРЫТ (F1b, F2–F6 verified), ПРОД ОБНОВЛЁН вручную до b475475 (6 PY-уроков live).
Builder-2 и Builder-3 умерли по сети на ~45-й минуте (Builder-3 успел написать F7 на диск,
НЕ закоммитил и не верифицировал). АДАПТАЦИЯ: такты укорочены до 2 фичей/билдера.
M3 ДОСТИГНУТ: F7 0af5351 · F8 dfecc4b (8 сегм, xray __get__) · F9 f97ce79 (5 сегм, дерево
от OSError) · F10 29e7770 (4 сегм, Pydantic-гейт; pydantic только в authoring-venv). Итого
10 уроков PY.M1–M10, census 37 карт ×2, MCQ 0%. M3-КОНТУР ЗАКРЫТ: монитор чист, evaluator
ПРИНЯТО (R-M3-evaluator.md; worktree-изоляция сработала) → F7–F10 = verified.
Builder-6 (лимит сессии): F11 закоммичен 2586b0e. Builder-7 (сеть): F12 финализирован и
закоммичен b56aeb2 (F11+F12 self-pass в features), фиксы M3 сделаны наполовину БЕЗ коммита
(py-exceptions/py-object-model/seed/M8_c4/lesson.css ghost/_fit-margins — на диске).
M4 ЗАКРЫТ: монитор чист (флейк verify записан честно), evaluator ПРИНЯТО (R-M4: 3×verify
полным логом = 3/3 GREEN, флейк не воспроизведён; 27/27 цитат verbatim) → F11–F14 verified.
ВСЕ КОНТЕНТ-ФИЧИ ВОЛНЫ verified. ДИСПОЗИЦИЯ по «dis-панель простаивает M6–M13» (СУЩЕСТВЕННОЕ
R-M4): НЕ фикс-волна — signature-ось жива в M1/M4/M5/M11 (dis) и рентгенах каждого урока
(cells/gi_frame/__get__/cr_frame/GIL); дозированные dis-кадры M6/M7/M9 → Backlog волны 2.
ФАЗА 5: фан-аут ЗАВЕРШЁН — E1 гейты ПРИНЯТО (все PASS) · E2 детали ПРИНЯТО-С-ЗАМЕЧАНИЯМИ
(паспорт PASS) · E3 стресс ПРИНЯТО (0 блокеров) · consumer-казуал ЦЕННОСТЬ (6/6) ·
consumer-ломатель ДОШЁЛ. Фикс-такты: b3c50a4 (консоль pre-wrap, org-атрибуция, s7,
сабтайтл, подзаголовки) + 6e1406c (hero=первая карта сессии ×3 состояния; (д)=рейт-лимит,
не баг; (в)=предсуществует P2). Полная регрессия зелёная. ТЕКУЩЕЕ: (1) push 6e1406c → CI;
(2) мой выборочный аудит доказательств (случайные машинные гейты своими руками);
(3) VPS-докатка по прецеденту + прод-смоук; (4) сведение таблицы гейтов → финальный отчёт
+ ретро → DONE-WAVE-1. Внешний эффект push/деплой преавторизован (Полномочия ТЗ + «залей
на прод»); компенсация прежняя: откат = образ codex-mini-app:b475475 / codex_old.
Счётчик майлстоунов: 4/5.
ФИКСЫ M3 в такт Builder-7 (из R-M3-evaluator + монитора):
- PY.M8_c4.py печатает литерал «AttributeError» вместо type(e).__name__ (буква RS-03 §6);
- F9 s2: усечение цитаты compound_stmts маркировать («…exception object[…]»);
- features.md F8: счётчик PNG 13→19 (точность счётчиков приёмки);
- прибрать мусорные untracked docs/evidence/{PY-CM,PY-EXC,PY-HINTS,PY-OBJ} (shot-скрипты
  писали во второй корень);
- verify/_fit-margins.mjs: порт 4173 хардкод → читать APP_BASE;
- ghost-узлы на reduced-motion финалах почти невидимы (пересъём M7 s4) — полиш если дёшево.
Для F11 (передано Builder-6 постфактум если нужно): signature dis-панель в M3 не
использована ни разу — в async-флагмане вернуть ось «невидимого» (timeline+GIL уже есть).
P2/волна-2: code-splitting данных уроков (бандл 123КБ gz, тренд +24КБ/4 урока, гейт 200КБ
пока с запасом). Замер FSRS-дозирования (43 new) — обязателен в F15.
Счётчик майлстоунов: 3/5 (норм; смерти сессий по сети лечатся короткими тактами).
NB: шумовые диффы docs/evidence/*.png от харнессов билдеры откатывают, не коммитят.
Мелочи M2 в такт 3: PEP492-цитата «[I]nternally»; aria-label чипа трека (сейчас
screen-reader читает «Python для AQAновый»); multicard-session расширить на 4-карточный
урок; конвенцию сокращения dis-аргументов задекларировать; держать headroom ≥6px (не
повторять 4.8px); долю 2-сценных чип-сегментов не наращивать; бандл смотреть на M4.
Счётчик майлстоунов: 2/5 (бюджет в норме: M2 за один такт).
Замечания критика в BUILD: c24 «speedup>3x» в карточки не брать; census-лог stdout/stderr
раздельно; шпаргалки F12/F13 без обязательной лесенки; FSRS-замер в F15; s6 = «деталь
CPython», не гарантия спеки.
Счётчик майлстоунов: 1/5 (бюджет-чекпойнт: скорость в норме — F1 за один прогон билдера).

## Фиче-борд (зеркало features.md)
F1 скелет: мультитрек+names-objects [M1, gold] · F2 collections-hash · F3 args ·
F4 closures [gold] · F5 decorators [gold] · F6 generators [M2] · F7 context-managers ·
F8 object-model · F9 exceptions · F10 type-hints [M3] · F11 async-await [M4, gold] ·
F12 шпаргалка strings+flow · F13 шпаргалка stdlib+idioms · F14 прогресс-по-трекам [M4] ·
F15 деплой [M5, gold]. Все todo.

## Синтез research (фаза 1)
1. Нарезка (RS-01): P0 7 уроков ≈43 сегмента (names-objects, closures-scope, decorators,
   generators, context-managers, async-await, object-model); P1 4 (type-hints, exceptions,
   collections-hash, args); P2 = справочные разделы md — НЕ уроки. 26 сниппетов проверены python3.
2. У всех 6 C#-уроков есть Python-пары (late-binding [2,2,2] ↔ closures «333» и т.д.) — мостики.
3. RS-02: 9 неточностей md (global/UnboundLocalError; полиморфизм-пример падает; иерархия
   исключений — OSError) — в уроках исправить; 6 senior-гэпов волны 1: GIL, refcount+GC,
   generator frames, closure cells, дескрипторы, small-int cache; банк 32 живых URL с цитатами.
4. Код (Explore): бэк мультитрек-ready (track из seed); фронт — малая доработка: группировка
   home (home.ts:132,210), лейблы (strings.ts:16,19,25), hlcode.ts C#-only → поле lang,
   бейдж «IL»→«dis» (strings.ts:196); карточная механика языко-агностична.
5. Python-исполнение: run-csharp не годится; эталоны = авторский python3.12 (3.12.13 есть
   локально; системный 3.9.6 не тянет современный синтаксис md).
6. Базлайн: dotnet 65/65, build чистый 59.84 КБ gz, CI зелёный, 6 уроков C#.
7. Макс/песс: ставка «видеть невидимое» (cells/frames/dis/GIL); риски — конфликт аудиторий,
   справочник-пустышки, точность упрощений, объём волны (всё → в brief и на гейт).

## Синтез research
<после фазы 1>

## Фиче-борд (зеркало features.md — там источник правды)
| F | Название | Майлстоун | Золотой путь | Статус | Раунд оценки |
|---|---|---|---|---|---|

## Диспозиции замечаний M1-evaluator (вердикт ПРИНЯТО, R-M1-evaluator.md)
- СУЩЕСТВЕННОЕ: viz-fit.mjs:42–49 browser-часть хардкодит 6 C#-уроков (слепая зона G4 для
  волны) → ФИКС-ЗАДАЧА builder-у ПЕРВОЙ строкой такта M2: список из реестра LESSONS.
- new-lessons.mjs: добавить assert predict-гейта (факт истинен, assert-а нет) → такт M2.
- Лесенка карточек: решено — c4 modify-предикт в PY.M1 в такте M2; explain-MCQ точечно ≤25%.
- s1 плотность на нижней границе + s7 подрезка первой строки код-панели + устаревший принт
  «all 6 lessons» в сводке viz-fit → полиш в такте M2.
- s6 «немедленно и детерминированно» при refs=0 → канон волны: подавать как деталь CPython,
  не гарантию спеки (builder-у в промпт M2).
- Consumer M1 вернул пустой отчёт (обрыв структурного вывода) → перезапущен свежим агентом.

## Диспозиции находок consumer-ломателя ФИНАЛА (такт пройден; 30 скринов)
- (а) HERO ВРЁТ: «Сессия на сегодня: Boxing и unboxing» → «Начать сессию» открывает
  PY.M1 (сессия смешивает треки — эффект волны) → ФИКС-ФИЧА перед деплоем: заголовок
  hero = реальная первая карточка сессии (или сессия стартует с hero-урока).
- (б) XP-ферма (переответ карточки пройденного урока даёт +XP повторно) — ПРЕДСУЩЕСТВУЕТ
  (воспроизведено на boxing/C#) → Backlog продукта P2, не волна.
- (в) XP шапки 12 vs дом 10 — проверить предсуществование в фикс-такте; вероятно P2.
- (г) потеря ответа при уходе до оценки — уже дispositioned (M1, Backlog продукта).
- (д) один ран «Завершить сессию» молча упал (без экрана завершения) — проверить в
  фикс-такте: воспроизводимость + не 429 ли рейт-лимитера; воспроизводимо → фикс.
- Плюс UX-раздражение: возврат в урок всегда сверху при видимом «Просмотрено 14%» —
  Backlog продукта (scroll-restore).

## Диспозиции находок consumer-казуала ФИНАЛА (такт пройден, 6/6 карточек, вовлечение
на 2-й урок; полный отчёт в journals ниже)
- (в) БАГ ВОЛНЫ: источник python.org подписан «ИСТОЧНИК · MICROSOFT LEARN» (урок
  collections-hash, Tutorial-ссылка) → ФИКС-ФИЧА перед деплоем (атрибуция = зона G2).
- (д) сабтайтл Python-секции «6 механизмов…» при 13 уроках → фикс там же (честное число
  или без числа).
- (г) hero «Сессия на сегодня» на Python-табе предлагает C#-урок «56 карточек · ~56 минут»
  → следствие решения «hero глобальный» + «стены» дозирования; НЕ чинится молча (петля
  LOCKED) → слабое место №1 отчёта + волна 2 (дозатор new/день сделает hero человечным).
- (а) тупик низа урока (кнопка карточки в середине) и (б) нет видимого фидбека на
  уточнение «Легко» после авто-оценки → проверить предсуществование; вероятно продуктовый
  Backlog P2 (как кнопки-active из M1), НЕ волна.
- (е) стрик/вчерашний прогресс в демо-режиме — ограничение стенда, не дефект.

## Диспозиции находок consumer-а M1 (такт пройден, ценность получена)
- (а) прогресс карточки теряется при закрытии до клика по оценке → ОТКЛОНЕНО для волны
  (довод-факт: review шлётся по grade-клику и в C#-уроках так же — предсуществующая
  механика продукта, волна её не трогает) → Backlog продукта P2.
- (б) счётчик сессии «1 из 3»/«2 из 2» + «Завершить сессию» до 3-й карточки при
  МНОГОкарточном уроке → ФИКС-ЗАДАЧА такта M2 (наша волна вводит 3+ карточки/урок —
  проверить sessionQueue на multicard, счётчик должен быть честным).
- (в) шапка «6 механизмов», а урок один → ОТКЛОНЕНО (временное состояние: после F2–F13
  уроков будет 13; довод-факт — волна в процессе).
- (г) кнопки оценки активны после выставления → M2: проверить, предсуществующее ли;
  новое → фикс, старое → Backlog продукта.

## Карта рисков / сомнения агентов
- Риск №1 (унаследован от продукта): ТОЧНОСТЬ контента — каждое утверждение сверять с
  первоисточником (docs.python.org), ответы карточек = реальный вывод python3.
- Риск: аудитория md = AQA-собес («на собесе спросят»), аудитория приложения = сеньор-глубина.
  Тон/акценты урока калибровать на гейте (сохранять ли собес-фрейминг).
- Риск: объём md (15 разделов) >> разумной волны уроков — нарезка/приоритет на гейте.
- Риск: карточки Python не могут использовать run-csharp — эталоны считать локально python3,
  зафиксировать в ТЗ как авторский гейт G-EXEC-PY.

## Backlog (МЕЛОЧИ / P2-хвосты)
- GIL/free-threading (3.13) ОТДЕЛЬНЫМ уроком — не в волне (глубокие GIL-сегменты в F11);
  кандидат волны 2 (решение design-фазы, критик потребовал не терять — зафиксировано).

## Журнал (append-only, не редактировать прошлые строки)
- 2026-07-17 — ФИНАЛ ВОЛНЫ 1. F15 закрыт: пейсинг харнесса bcdaba4 (корень CI-флейка =
  429 лимитера, подтверждён расчётом 63>60/мин); ребейз поверх юзерских ci-коммитов
  (0c9d929 deploy-hub, 8a5fb59 app id); мой фикс permissions {packages:write} по контракту
  хаба (dfa39ea); CI 29548427723 test+deploy SUCCESS; прод: контейнер healthy, бандл
  index-DwjJNnuW.js == локальная прод-сборка main БАЙТ-В-БАЙТ, 13 PY-уроков. Выборочный
  аудит оркестратора: 3 карточки python3.12 == census; viz-fit ALL GREEN своим прогоном;
  lint: gates чист, state-находки формата ревью — в ретро. [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-17 — ИТОГ: класс=контентная волна software-UI (improvement) | режим=полный |
  планка=готовый продукт | майлстоуны 5/5 (прогноз 5) | фикс-волны: 3 микро-такта фазы 5 |
  дедлоков 0 | ступень=3 (см. отчёт; signature процитирован 1 персоной дословно) |
  ретро-вердикт: ПОЛУЧЕН — волна 2 = «Собес-тренажёр (блоки 10–11)»: открытые вопросы
  (explain-колоды со структурным каркасом ответа) + STAR-кейсы с длинным контекстом.
  ПРЕДУСЛОВИЕ волны 2 (инвентарь контента): md-файлы блоков 10–11 конспекта у пользователя
  (в репо их НЕТ — только 00-python-refresher.md); запрошены. Дозатор new/день — кандидат
  №2 (вошёл бы в волну 2 как пререквизит комфортной петли — решить на её гейте).
  РЕТРО (что контур не увидел вовремя): (1) хардкод sourceKicker «Microsoft Learn» жил
  3 майлстоуна — G2-аудиты сверяли цитаты, но не UI-атрибуцию; поймал только consumer →
  канарейка: в detail-pass добавлять «подписи источников на экране = sources уроков»;
  (2) CI-флейк 429 диагностирован на 3-й встрече — харнессы, растущие с контентом, надо
  проверять на бюджет rate-лимитера при каждом росте; (3) сетевые смерти долгих builder-
  сессий → короткие такты по 2 фичи оказались устойчивой нормой; (4) lint-формат ревью
  (вердикт в начале файла) агенты нарушали систематически — кандидат на правку промпта
  роли evaluator (диффом, на утверждение пользователю системы).
- 2026-07-16 — CI run 29450728387 (HEAD b475475): test+build-and-push SUCCESS, но шаг
  SSH-деплоя SKIPPED — секреты VPS_* в репо не заданы (graceful-skip). Прод не обновился
  автоматически. У машины есть прямой SSH (host vpn = racknerd, контейнер codex, том
  codex_data, порт 127.0.0.1:8091, restart unless-stopped, docker run без compose).
  РУЧНАЯ ДОКАТКА (авторизация: дословное «а мож залить что есть на прод»): pull
  ghcr.io/dreamsareal/codeapp:<sha b475475> → rename codex→codex_old (stop) → docker run
  новый codex с ТЕМ ЖЕ томом codex_data:/data, портом и токеном (из env старого контейнера,
  не печатается). КОМПЕНСАЦИЯ до исполнения: откат = stop codex && rename codex_old→codex
  && start (старый образ и контейнер сохраняются до финала волны). Том с данными не
  трогается ни в одном сценарии. [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-16 — ДОКАТКА НА ПРОД ЗАВЕРШЕНА (b475475). GHCR-пулл не удался (пакет приватный,
  у gh-токена нет read:packages) → путь: git archive b475475 → сборка НА VPS
  (codex-mini-app:b475475) → swap. Попытка 1 упала: SQLite «attempt to write a readonly
  database» — том codex_data принадлежал root, а hardened-образ работает под USER app
  (uid 1654); откат по компенсации сработал (старый контейнер, health ok). Фикс: атомарный
  swap с chown тома под 1654:1654 → health ok, контейнер healthy. ДОКАЗАТЕЛЬСТВА: бандл
  прода отдаёт «Python для AQA» + все 6 PY-уроков (PY.M1..M6) в assets/index-BIDhcuLu.js;
  /api/lessons 401 без токена = штатный прод-локдаун. ДАННЫЕ ЦЕЛЫ (том не пересоздавался).
  ОТКАТ-АРТЕФАКТЫ до финала волны: контейнер codex_old (stopped) + образ
  codex-mini-app:latest; обратный откат = rm codex, chown 0:0 тома, rename+start codex_old.
  NB для CI-автодеплоя: нужны 5 секретов в GitHub (VPS_HOST/USER/SSH_KEY/PORT +
  TELEGRAM_BOT_TOKEN) — задать может только пользователь. [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-15 — ПОЛЬЗОВАТЕЛЬ: «а мож залить что есть на прод?» → промежуточный деплой
  разрешён дословно. Внешний эффект: git push origin 64fa127:main (F1+F1b+F2+F3, все
  коммиты после зелёных харнессов билдера; F1 verified, остальное self-pass — CI
  перепроверит на Linux). КОМПЕНСАЦИЯ до исполнения: откат = деплой предыдущего
  sha-образа 5772ad5 (CI пинит образы по sha; на VPS docker-compose перепин на
  ghcr-тег 5772ad5) — механический, без revert-коммитов. Builder работает дальше,
  его незакоммиченный F4-прогресс пушем не затронут. [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-15 — PUSH OK: 5772ad5..64fa127 → main (1-й таймаут был из-за тяжёлого пака
  ~354MiB history; ретрай прошёл). CI-прогон запущен, слежу через gh. [ЭФФЕКТ-ПРИМЕНЁН]
- 2026-07-16 — CI run 29446600520 FAILURE: viz-fit на Linux, 1 FIT: PY.M3 s4/srcl
  «[1, 2, 3, 4]» len=113.34 > avail=110 (FreeType шире CoreText). Деплой НЕ случился,
  прод цел (образ 5772ad5). Builder-у отправлена приоритетная вставка: структурный фикс
  (компактный моно-лейбл) + профилактика запаса ≥6px по всем PY-урокам + отдельный коммит
  → я перепушу. Остальные проверки CI по всем 9 урокам зелёные (CLIP/OVERLAP/лестница/
  ортогональность/mid-transition 303 замера — 0 нарушений).
- 2026-07-15 — INTAKE: новая задача поверх DONE-продукта → мини-пайп docs/tasks/python-track/.
  Класс = контентная волна software-UI (improvement), флаги выставлены, гайды прочитаны
  (GUIDE.md §2-§5, AUTHORING-AI.md целиком), STATE создан. Продуктовый слой docs/ не тронут.
