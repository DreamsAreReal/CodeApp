# R-final-E2 — детали/перцептивная рубрика/паспорт вкуса (финальный VERIFY, прогон E2)

Дата: 2026-07-17 · HEAD: 7330743 · Прогон: внешний evaluator E2 «ДЕТАЛИ» (свежий контекст)
Метод: собственные headless-скрины 390×844 всех 13 py-уроков (Chromium, deviceScaleFactor 2),
финальные кадры КАЖДОГО сегмента + gate-кадры + карточки; скрипты `.verify/e2-final-shots.mjs`,
`.verify/e2-rest-shots.mjs`; доказательства — `reviews/evidence/final-E2/` (118 PNG).
Сверка dist↔HEAD: dist собран 03:52 после коммита 7330743 (03:50); `PY.M13` в бандле (grep=6),
ghost `opacity:.4` в собранном CSS. Консоль браузера: **0 ошибок** на обоих прогонах
(names+async walk и 11-уроков walk — вывод скриптов «console errors: 0»).

---

## Построчный разбор по чеклисту E2

### 1. Типографика / вёрстка (390px)

Перечислено и сверено по моим кадрам (не билдерским):

| Проверка | Где смотрел | Итог |
|---|---|---|
| Обрезанные лейблы в виз-узлах | все финалы 13 уроков (final-E2/*.png) | не найдено; подозрения «AsyncCClient» (names-s1) и «картеж» (clos-s5) сняты зумом ×2 + источником (names-objects.ts:106 «AsyncClient», closures-scope.ts:278 «кортеж») |
| Слипшиеся элементы / переполнения | om-s1…s8, exc-s1…s5, coll, clos, deco, gen, cm, args, hints, str, std | не найдено; zones-подписи (ИМЕНА/ОБЪЕКТЫ/РЕНТГЕН…) читаемы |
| Прыгающие базлайны | код-панели + dis-панели (names-s8, clos-s5, deco-s8) | моно-строки ровные, подсветка активной строки согласована |
| Кириллица/латиница внутри слова | grep-скан всех py-*.ts (без дефисных пар) | 0 совпадений; гибриды только через дефис («wrapper-ы») — паттерн продукта |
| Моно-код читаем | все код-панели, JetBrains Mono, 2×DPR | да |
| Home-карточки | home-python.png | подзаголовки эллипсис на середине слова — см. ПРОБЛЕМА 3 |
| Консоль-стрип | str-s1-final.png, async-s7-final.png | **многострочный stdout схлопнут в одну строку** — см. ПРОБЛЕМА 1 |

### 2. Голос / копирайт

- Тон «ты» повсеместно: «под твоим стеком AQA» (names-s1), «держи правило» (closures.ts:180),
  «Уверен в ответе?» (std-card.png). Обращений «вы/вас» — 0 (grep с фильтром ложных срабатываний).
- Сеньорский, без сюсюканья: explain-блоки везде «механизм → почему», EN-цитаты дословные
  с первоисточником (names s7 PEP 8, om s4 super proxy, async s6 GIL/free-threading 3.13).
- Глоссарий ТЗ: «имя ссылается на объект» — names hook дословно высмеивает «переменная —
  коробка со значением» как анти-паттерн (names-head.png); mutable/immutable EN в коде,
  рус. в прозе; байткод-панель — «DIS · БАЙТКОД исполняет CPython», «IL» в py-уроках
  отсутствует (grep `\bIL\b` по py-*.ts = 0).
- Сравнения с C#: grep «C#|.NET|CLR» = 1 хит — EN-комментарий кода (names-objects.ts:23,
  не user-facing). Глазами в 60+ кадрах и explain-текстах — 0 врезок «как в C#». PASS.
- Собес-блоки: misconceptions «finally — это "просто выполнится в конце"» (py-exceptions.ts:82)
  — ответ обоснован механизмом + PEP 765, не зубрёжка. Соответствует решению гейта.

### 3. Перцептивная рубрика

- **Кадры несут состояние**: autoplay-кадр ≠ final-кадр на проверенных парах
  (async-s6: «CPU-bound / ждёт GIL» → «I/O / GIL отпущен + байткод у потока 2»;
  async-s7: гейт «луп заморожен»+«не бежит» → «пауза отдаёт луп»+«во время сна»;
  names-s8: LOAD_NAME x / refs:1 → STORE_NAME y / refs:2). PASS.
- **Вердикт-кадры не пустеют**: names-card-after.png — «✕ мимо», «твой ответ» vs
  «ожидалось» диффом + ключ-объяснение + шкала оценки. PASS.
- **Refs-чипы/гейты/рентгены читаемы**: refs: 1→2 (names s2/s8), РЕНТГЕН · __closure__
  (clos-s5), РЕНТГЕН · gi_frame (gen-s2), РЕНТГЕН · cr_frame (async-s5),
  РЕНТГЕН · __annotations__ (hints-s2). PASS.
- **Ghost-узлы после фикса 0.4**: styles/lesson.css:539-542 (opacity 0.4 + коммент)
  и в собранном CSS; глазами: старый объект 'py' (names-s3), «other/не бежит»
  (async-s7-autoplay), пунктирный «обычный dict» (hints-s2) — бледные, но читаемые. PASS.
- **Coral дозирован**: в рядовых кадрах — да (1-3 акцента на кадр). Исключение —
  names s1 финал: ковёр из 10 сплошных coral-чипов (см. ЧТО ПЛОХО 1).

### 4. Консистентность трека

- Kicker-стиль уроков единый: 11× «Python · <тема> · механизм», 2 шпаргалки —
  «Python · шпаргалка · <тема>» (осознанная дифференциация формата). PASS.
- Home: переключатель «Фундамент C# | Python для AQA (новый)», активный — коралл;
  подзаголовок группы «6 механизмов, из которых сделаны твои инструменты»; у каждой
  из 13 карточек внятный подзаголовок (обрезка хвостов — ПРОБЛЕМА 3). PASS.
- Порядок = concept-DAG: index.ts:45-57 names→collections→args→closures→decorators→
  generators→CM→object-model→exceptions→hints→async→strings→stdlib; на home тот же
  порядок (home-python.png). Совпадает с чеклистом дословно. PASS.

### 5. Ставка вау («сеньор видит невидимое») — пройдено глазами на :4173

- py-names-objects: dis-панель s8 с реальными опкодами 3.12 (RESUME/LOAD_CONST/
  STORE_NAME/LOAD_NAME/RETURN_CONST), активный опкод подсвечен и меняется между
  кадрами; refs-счётчик на объекте анимирован (1→2 на s2 и s8, «сам себя» цикл s6);
  int-cache s7 (257: два объекта, is → False). ПОКАЗАНО, не упомянуто.
- py-async-await: event-loop timeline (s1), корутина-объект с «тело НЕ бежит» (s2),
  cr_frame-рентген с n=42 и IP (s5), GIL-жетон, переезжающий между потоками
  (s6 autoplay vs final — разные владельцы), антипаттерн time.sleep с predict-гейтом
  (s7). ПОКАЗАНО.
- Signature-элемент паспорта (dis-панель + невидимые сущности CPython) присутствует
  и заметен минимум в 5 уроках (names s8, clos s5, deco s8, gen s2, async s5). PASS.

---

## ВЕРДИКТ: ПРИНЯТО-С-ЗАМЕЧАНИЯМИ

**Паспорт вкуса: PASS** — доказательства: reviews/evidence/final-E2/home-python.png
(cream #F6F1E9, белые карточки, coral-акценты, Rubik/Onest, радиусы, line-SVG, бейдж
«новый»), names-s8-final.png и clos-s5-final.png (signature dis-панель заметна),
async-s6-final.png (GIL-жетон). Анти-слоп: золота/тёмного-кино/детсад-зелёного/Inter/
эмодзи-иконок — нет ни в одном из 118 кадров.

ПРОБЛЕМЫ:
- [СУЩЕСТВЕННОЕ] Консоль-стрип теряет переводы строк: 32 многострочных `out` в 10
  py-уроках (grep `out: "…\n` — py-strings-flow 5, py-object-model 7, py-async-await 6,
  py-context-managers 5 и др.; в C#-треке — 0, дефект внесён волной) рендерятся одной
  строкой через пробел: vizPlayer.ts:534 пишет `textContent`, а `.vz-cout`
  (lesson.css:568) без `white-space: pre-wrap`. Скрин: final-E2/str-s1-final.png —
  консоль «es t tseT Ts» вместо четырёх строк stdout; async-s7-final.png — «block start
  other block end» слитно. Нарушено: перцептивная рубрика «кадр несёт состояние»
  (строчная структура stdout — само содержание урока про print) + рассинхрон с
  карточным контрактом «введи ровно то, что напечатает программа» (expect с `\n`).
  Фикс: `white-space: pre-wrap` на `.vz-cout` (+ выравнивание flex-строки) — одна
  CSS-строка, но пройтись глазами по всем 32 кадрам после.
- [МЕЛОЧЬ] py-async-await s7, сцена 3 «починка» (py-async-await.ts:404-415): код-панель
  по-прежнему показывает `time.sleep(0.05) # sync-вызов!` с подсветкой, а консоль и
  подпись описывают прогон с `await asyncio.sleep` — в одном кадре код противоречит
  консоли; спасает только чтение подписи. Скрин: async-s7-final.png. Фикс: пометка
  «код после починки» в кадре или снятие подсветки строки.
- [МЕЛОЧЬ] Home 390: подзаголовки py-карточек эллипсис на середине слова — «Компилятор
  красит имена; cells; l…», «Event loop, корутина-объект, gathe…», «Перепривязка имени,
  def-time, wr…» (home-python.png). Паттерн унаследован от C#-ленты, но у 10 из 13
  py-карточек хвост потерян. Фикс: сократить subtitle до ~45 знаков.
- [МЕЛОЧЬ] names-objects s1 (карта трека), финальная сцена: все 10 чипов залиты
  сплошным coral (names-s1-final.png, зум names-s1-top.png) — самый громкий кадр
  трека, «дозированность» коралла в этом кадре нарушена. Фикс: акцентировать только
  рёбра пар механизм→инструмент, чипы — outline.

ЧТО ПЛОХО:
- Опыт/петля: сегмент «карта трека» (names s1) — повтор слабости из R-M1-evaluator.md
  («самый слабый по плотности: почти статичная сетка») → по правилам анти-зацикливания
  это второй вердикт подряд с тем же пунктом — эскалация оркестратору как кандидат в
  фикс-фичу. Рядом с плотностью boxing-эталона этот сегмент — декорация.
- Опыт/петля: мостик «сегмент → карточка» на шпаргалках слабее референса: сегмент
  показывает stdout слитной строкой (ПРОБЛЕМА 1), а карточка требует напечатать его с
  переводами строк — ученик получает противоречивый образец прямо перед вводом.
- Шпаргалки M12/M13 — по 3 сегмента, ровно на нижней границе порога G3 для шпаргалок;
  идиомы truthiness/walrus/zip живут только в карточках и takeaways — это законно по
  ТЗ (P2), но заметно тоньше ядра волны.
- Мелкие подписи зон (small-caps, muted #8A8377) на 390px близки к нижнему пределу
  читаемости при ярком солнце (TMA-кейс «в поездке») — AA-контраст E1 меряет, но
  запас не визуальный.

ПРОВЕРКА ДОКАЗАТЕЛЬСТВ:
- «ghost-узлы видимы после фикса 0.4» → перепроверено: lesson.css:539-542 + собранный
  CSS (`opacity:.4`) + глазами на 3 кадрах → подтверждено.
- «0 ошибок консоли» (features.md, многократно) → мои 2 прогона по всем 13 урокам:
  0 ошибок → подтверждено.
- «13 уроков, порядок DAG, home двумя треками» → реестр + мой home-python.png →
  подтверждено.
- «бейдж dis вместо IL в py-уроках» → «DIS · БАЙТКОД исполняет CPython» на names-s8,
  clos-s5, deco-s8 → подтверждено.
- Свежесть preview против HEAD → dist 03:52 > commit 03:50, PY.M13 в бандле →
  подтверждено (скрины сняты с HEAD, не со старой сборки).

СОМНЕНИЯ:
- Часть моих «final»-кадров длинных уроков (om, exc) поймана в mid-state — реплей
  автозапуска при обратном скролле; состояние сегментов судил по совокупности
  autoplay+final+источник сцен. На вердикт не влияет, подсчёт сцен — зона E1.
- Console-стрип, возможно, задумывался как «последняя строка вывода», но данные волны
  кладут в него полный многострочный stdout — тогда это конфликт контракта данных с
  движком, а не CSS-баг; в любом случае текущий рендер вводит в заблуждение.

РЕЗЮМЕ: Волна выдерживает планку продукта: паспорт вкуса PASS, ставка «сеньор видит
невидимое» реально показана (dis/refs/cells/gi_frame/cr_frame/GIL — анимацией, не
словами), голос и глоссарий чистые, 0 сравнений с C#, порядок трека = DAG, 0 ошибок
консоли. Одно СУЩЕСТВЕННОЕ — схлопывание многострочного stdout в консоль-стрипе
(внесено волной, лечится одной CSS-строкой + перепросмотр 32 кадров) — рекомендую
закрыть до/сразу после релиза; три мелочи — в backlog.
