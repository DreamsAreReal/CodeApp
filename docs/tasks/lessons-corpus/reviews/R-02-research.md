# R-02 — Ревью ресерча (фаза 1.3), задача lessons-corpus

Дата: 2026-07-17. Критик: adversarial critic конвейера.
Объекты: RS-00, RS-01, RS-baseline, RS-02a, RS-02b, RS-03, RS-04, проба `.spikes/probe/`.

## ВЕРДИКТ (раунд 2): ПРИНЯТО — с обязательными условиями к фазе ТЗ

Раунд 1 (ниже, сохранён для трассировки): ДОРАБОТАТЬ, блокеры Б1–Б5.
Раунд 2: Б1–Б4 закрыты исполненными артефактами (перепроверено независимо),
Б5 понижен до условия синтеза. Ресерч пригоден как вход для ТЗ.

### Перепроверка блокеров (раунд 2, по файлам и независимыми командами)

- **Б1 (проба) — ЗАКРЫТ.** `.spikes/probe/REPORT.md` существует: tsc exit 0,
  pure-прогон реального `layoutScene` (7 сцен, 0 нарушений, с честной историей
  RED→GREEN — первый драфт движок завалил), цена урока с разбивкой дорогих частей,
  якорь **6–10 exec-уроков/майлстоун ~40 ч**. Оговорки честные (токены — оценка,
  цитаты не фетчились, viz-fit покрывает только зарегистрированные уроки).
- **Б2 (Гудхарт G-EXEC) — ЗАКРЫТ в пробе, открыт как дизайн-вопрос.** Фальшивый
  `verify.run: "echo …"` убран (проверено grep: обе карточки → `expect: ""` =
  легальный MCQ-fallback по types.ts:90 + AUTHORING-AI §9). Ключевая находка
  зафиксирована: для non-exec доменов generation-карточки сегодня НЕВОЗМОЖНЫ —
  либо новый механизм verify, либо осознанное снижение планки. Плюс четыре
  конкретных риска конвейерной деградации (включая ревью-чек «verify.run не
  содержит ответ»).
- **Б3 (RS-03) — ЗАКРЫТ.** v2: методика из 3 каналов (curl 141 URL: 65×200/8×404/
  68×429; дерево `dotnet/docs` 28k файлов; `dotnet-api-docs` для 23 API-страниц),
  таблица 17 замен битых URL. Выборочная перепроверка критиком: 2 заменённых URL
  (hashtable-and-dictionary-collection-types, delegates-overview) — живые ✔.
  Добавлены 5 разделов (+24 урока): коллекции (hashtable сохранён — §4 матрица
  анти-регрессии по всем 6 удаляемым урокам), yield, System.Text.Json, Channels,
  IO/Pipelines. Арифметика сведена: 15 core = 108, 6 optional = 33, итого 141 —
  пересчитано критиком, сходится ✔.
- **Б4 (базлайн) — ЗАКРЫТ.** RS-baseline v2: все 5 харнессов ALL GREEN + dotnet
  test 65/65 + живой G-EXEC curl, выводы приложены. Карточек **59** — подтверждено
  критиком независимым grep (`"id": "c\d+"` в сиде = 59; фронт `verify: { kind:
  "exec"` = 60 − 1 в types.ts = 59) ✔; v1-число 84 действительно было артефактом
  грязного grep. Попутно добыты критичные для ТЗ факты: due дня 1 = весь каталог
  (лимита новых карт НЕТ), rate-limiter 60 POST/мин, харнессы run.mjs/new-lessons.mjs
  завязаны на удаляемые C#-уроки (их правка — часть скоупа удаления).
- **Б5 (объём) — ПОНИЖЕН до условия.** Внутреннее противоречие RS-03 v1 снято
  (141 сведён с таблицей). Сводная таблица потолка (~520–650) обещана в синтезе
  оркестратора — это обещание, не артефакт. Условие ниже.

### Обязательные условия к ТЗ (иначе вернутся блокерами на ревью ТЗ)

1. **Сводная таблица объёма** (источник → уроков min/max → волна) в синтезе
   STATE/ТЗ; строку RS-04 «C# трек 15–20 уроков» пометить устаревшей (истина —
   RS-03: core 108).
2. **Механизм верификации non-exec уроков** — решение в ТЗ (новый verify /
   осознанный MCQ-потолок / песочница), с гейтом «verify.run не содержит ответ».
3. **Лимит новых карт/день или порционная выдача** — дизайн-решение до волны
   наполнения (59 карт в due дня 1 уже сейчас; сотни — неюзабельно).
4. **Правка харнессов** run.mjs/new-lessons.mjs — в скоуп удаления C#-уроков.
5. `LessonLang` не имеет значения для non-code уроков (Docker) — расширение схемы
   в фиче-лист.

### Некритичный остаток (не блокирует, зафиксировать в backlog)

- RS-04 НЕ правился: «NUnit v5+» (опровергнуто вживую: актуальна 4.6.1),
  «官方документации», слой практиков не пробит (Dapper на одном codewithmukesh),
  JetBrains-опрос декоративен. Таблицы «инструмент → 2 источника» нет.
- 60 URL RS-03 подтверждены только деревом репо (429 у Learn) — риск низкий,
  честно помечено в §7.
- OCR-проба для 4 нечитаемых книг (Кормен, Фаулер, EIP, Седжвик) не выполнена —
  явный вопрос гейта ТЗ с ценой обоих вариантов (OCR vs срез).
- Токены/урок — эвристика, не замер (помечено «уверенность низкая» — приемлемо).

### ЧТО ПЛОХО (обязательный минимум даже при ПРИНЯТО)

1. RS-04 остался в состоянии раунда 1 — единственный отчёт без v2; его слабый
   провенанс всплывёт при дизайне трека инструментов.
2. Цена урока для non-exec доменов неизвестна в принципе (в пробе самые дорогие
   части — fetch-верификация цитат и G-EXEC — оценены, не измерены); первый
   майлстоун волны наполнения должен быть калибровочным.
3. Противоречие «RS-00: 21 урок» vs факт 19 так и не исправлено в RS-00 — мелочь,
   но синтез обязан брать числа из baseline v2, не из RS-00.

---
---

# Раунд 1 (история): ДОРАБОТАТЬ

Корпус ресерча неоднороден: RS-02a/b — образцовые (закрытый корпус 20/20, методы
исполнены, деградации и противоречия честно зафиксированы); RS-01/baseline конкретны
по формату движка; но проба исполнимости не существует как отчёт, RS-03 непрослежен
и дыряв, базлайн не снят инструментами будущих гейтов, а суммарный объём корпуса
противоречив между отчётами. ТЗ на этих данных получит выдуманный якорь объёма
и фальсифицируемый ключевой гейт (G-EXEC для non-exec доменов).

## Перепроверено вживую (раунд 1)

- **C# 15 (RS-03)**: страница `learn.microsoft.com/.../whats-new/csharp-15` существует;
  union types, closed hierarchies, memory safety — реальные фичи preview .NET 11. ✔
- **Stack Overflow 2025 (RS-04)**: C# 27.8%, ASP.NET Core 19.7%, SQL Server 30.1%,
  Azure 26.3%, NuGet 18.9% — подтверждены. ✔
- **CQRS.Mediatr.Lite (RS-04)**: репозиторий Microsoft существует. ✔
- **NUnit «v5+» (RS-04)**: ОПРОВЕРГНУТО — актуальная версия 4.6.1 (май 2026). ✘
- **URL RS-03** `dotnet/standard/threading/async-programming/`: HTTP 404. ✘
- **`.spikes/probe/`**: `REPORT.md` на момент раунда 1 НЕ существовал. ✘

## БЛОКЕРЫ раунда 1 (статус на раунд 2 — закрыты/понижены)

### Б1. Проба исполнимости без отчёта и без доказательств → ЗАКРЫТ
`.spikes/probe/REPORT.md` отсутствовал; probe-урок draft, не собран, не прогнан,
цена урока не измерена. Нарушен принцип «кандидаты — с артефактами спайков».

### Б2. Проба фальсифицирует ключевой гейт G-EXEC (тест Гудхарта) → ЗАКРЫТ
`verify: { run: "echo '3'", expect: "3" }` — гейт проходился эхом ответа; шапка
лгала «REAL docker build output». Механизм верификации non-exec доменов (~2/3
корпуса) не был исследован.

### Б3. RS-03 непрослежен и неполон для «сеньор вглубь» → ЗАКРЫТ (v2)
≥1 URL 404, зонтичные URL на десятки тем, «100% первоисточник» без артефактов;
не было collections internals (тема удаляемого hashtable исчезала = регрессия),
System.Text.Json, Channels, IO, yield.

### Б4. Базлайн снят НЕ инструментами гейтов → ЗАКРЫТ (v2)
Исполнен был только `npm run build`; verify-харнессы, dotnet test, G-EXEC — ни разу.
«FROZEN» замораживал несуществующие прогоны.

### Б5. Противоречия объёма между отчётами → ПОНИЖЕН до условия синтеза
Сумма оценок ≈ 500–660 против якоря RS-00 300–600; RS-04 W1 «C# 15–20 уроков»
vs RS-03 core; RS-00 «21 урок» vs 19.

## НЕКРИТИЧНО раунда 1 (статус)

1. NUnit «v5+» — НЕ исправлено (остаток).
2. Слой практиков RS-04 — НЕ исправлено (остаток).
3. Арифметика RS-01/baseline (311 vs 328; «сегментов ~2.1»; 141 KB gzip = весь
   бандл) — ИСПРАВЛЕНО в baseline v2 (316 строк, 59 карт, gzip помечен).
4. OCR-проба 4 книг — остаток, вопрос гейта.
5. Поведение /api/due и лимит карт — ИЗМЕРЕНО в baseline v2 (лимита нет, 59 карт).
6. Слоп («官方», «переquerie») — частично исправлено (проба вычищена, RS-04 нет).
7. EIP год / главы Дэвиса — остаток, дёшево.

## Что хорошо (фиксация, чтобы не переделывали)

- RS-02a/b: 100% закрытого корпуса, честные методы (pdfplumber/pdftoppm/djvused
  с выводами команд), U+2019 в именах, секции противоречий — эталон.
- RS-00: пессимист-углы (масштаб, копирайт, non-exec, сироты БД) подтвердились
  обоими раундами ревью — синтез в ТЗ обязателен.
- RS-03 содержательно актуален (C# 15 preview — правда).
- Baseline v2 и REPORT пробы — теперь эталон формата «число = команда + вывод».
