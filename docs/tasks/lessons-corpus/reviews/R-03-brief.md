# R-03 — Ревью ТЗ (фаза 2.2), задача lessons-corpus

Дата: 2026-07-17. Критик: adversarial critic конвейера.
Объект: `docs/tasks/lessons-corpus/brief.md`. Эталоны: RS-00/01/baseline/02a/02b/03/04,
R-02 (условия к ТЗ), `.spikes/probe/REPORT.md`, quality-baselines.md.

Раунд 3 (финальный) — сверху; раунды 2 и 1 сохранены ниже для трассировки.

---

# Раунд 3: перепроверка правок раунда 2 — ФИНАЛЬНЫЙ ВЕРДИКТ

Протокол: каждая заявленная правка перепроверена по тексту brief.md, не по handoff-у.

## Разбор

### С-R2-1 (арифметика таблицы объёма) — ЗАКРЫТ ✔

brief.md:9–15. Пересчитано против источников:
- «Книги читаемые, 16 шт.; без Кормена: ~240–320» ↔ RS-02a/b: (190–260 − Кормен
  40–60) + (90–120) = 238–317 → округление честное ✔.
- «OCR: Кормен, Фаулер, EIP: ~70–100» ↔ 40–60 + 18–25 + 10–14 = 68–99 ✔.
- Итого «~480–690» ↔ 108+60+240+70 = 478 ≈ 480; 141+130+320+100 = 691 ≈ 690 ✔.
- «~60–100 майлстоунов» при 6–10 уроков/майлстоун: 480/10…690/6 = 48–115 —
  заявленный коридор внутри расчётного ✔.
Пометка «RS-04 УСТАРЕЛА, истина — RS-03 v2» на месте. Условие R-02 №1 теперь
выполнено полностью (форма + трассируемые числа).

### С-R2-2 (конвертированный опыт-пункт: траектория волны 1) — ЗАКРЫТ ✔

brief.md:130–131: новая секция «Требования к design.md (заказ фазе 3)» — первая
сессия нового пользователя, первый «вау»-урок, prereq-цепочки разделов, порядок
открытия, «проектируются в design.md до сборки». Требование адресует ровно тот
пробел, ради которого пункт конвертировался. ✔ (Числового порога «первый вау
≤ N минут» нет — оставляю сигналом design-у, см. «ЧТО ПЛОХО».)

### Регрессии раунда 2 — ЗАКРЫТЫ ✔

- brief.md:15–17: строка «Итого» — 4 ячейки, «Ключевые инсайты» вынесены отдельным
  абзацем; markdown валиден ✔.
- brief.md:128: открытый вопрос 3 — «~41 урок C# core», совпадает со скоупом
  (стр. 42) ✔.

### М-R2 (способ измерения контр-метрики G9) — ЗАКРЫТ ✔

brief.md:79: способ дополнен «скрипт-симуляция 14 дней очереди (по образцу
verify/loop.mjs: ежедневный due → grade → следующий день)», контр-метрика уточнена
до измеримой «due любого из 14 дней ≤25 карт». Образец loop.mjs существует и зелёный
(RS-baseline п.0.9) — способ реализуем. ✔

### Проверка на регрессии от правок раунда 3

Diff затронул: таблицу объёма, G9, вопрос 3, новую секцию требований к design.
Остальные секции (ставка вау, паспорт, гейты G1–G8, полномочия, базлайн,
guardrails) — без изменений с раунда 2, повторно сверены точечно: противоречий
с правками не внесено. ✔

### Пункт «конфликт ставки вау с дефолтом non-exec=MCQ» (повторялся в Р1 и Р2)

По правилу релизного веса опыт-пункт, повторившийся в двух вердиктах подряд,
подлежит конвертации в СУЩЕСТВЕННОЕ. Здесь фиксирую так: его фикс-фича УЖЕ в P0
(«механизм verify для non-exec ЛИБО явное решение non-exec=MCQ в design» + ADR
в рисках) — маршрут решения существует и санкционирован условием R-02 №2; новым
СУЩЕСТВЕННЫМ против не менявшегося и ранее принятого содержимого его не поднимаю.
Остаток пункта — «цена дефолта не названа» — конкретизирую как МЕЛОЧЬ-требование
к ADR (см. вердикт) и эскалирую оркестратору как кандидат в фикс-фичу дизайна
(механизм verify для инструментов — харнессы docker/psql — стоит спайка в фазе 3).

## Вердикт (раунд 3, финальный)

```
ВЕРДИКТ: ПРИНЯТО-С-ЗАМЕЧАНИЯМИ

ПРОБЛЕМЫ:
- [МЕЛОЧЬ] brief.md:122 (риски, ADR non-exec): ADR о дефолте non-exec=MCQ обязан
  назвать цену решения для потребителя — потерю generation-петли (ввод ответа →
  детерминированная сверка) для ~2/3 целевого корпуса — иначе гейт-решение будет
  приниматься вслепую — фикс: одна фраза-требование в ADR.
- [МЕЛОЧЬ, backlog из раунда 1 — без изменений, уходят в STATE.md]:
  (а) G4-lint ловит только литеральное вхождение expect в run — вычисленный ответ
  пройдёт; закрыть проверкой в G7-аудите;
  (б) выборка G7-аудита — без правила отбора; фикс: случайная, seed фиксирует
  evaluator, не builder;
  (в) порог G1 «начальный чанк ≤ базлайн +10%» после code-splitting переопределить
  в design («чанк без данных уроков ≤ X»).

ЧТО ПЛОХО:
- Опыт: «первый вау-урок» заказан design-у без числового порога (у референса
  boxing.ts машинная панель — на первом уроке первой сессии); если design не даст
  измеримую планку («первая машинная панель ≤ N минут от старта»), требование
  выродится в декларацию.
- RS-04 остаётся единственным отчётом без v2 (третий вердикт подряд): конвейерные
  «7 уроков» на инструмент, слой практиков не пробит — эскалирую оркестратору:
  пересборка реестра инструментов = обязательная часть подготовки волны 2, иначе
  design W2 унаследует картон.
- Ставка вау для non-exec доменов держится на неспроектированном механизме verify
  (вилка в design): при дефолте MCQ ось «уровень ниже абстракции» в 2/3 корпуса
  теряет главный такт петли — предсказание с проверкой.

ПРОВЕРКА ДОКАЗАТЕЛЬСТВ:
- Таблица объёма → пересчитана против RS-02a/b/03/04: 240–320 ↔ 238–317;
  70–100 ↔ 68–99; итого 480–690 ↔ 478–691 ✔
- «~60–100 майлстоунов» → 480/10…690/6 = 48–115, коридор внутри ✔
- Вопрос 3 «~41 урок» ↔ скоуп волны 1 (стр. 42) ✔
- G9-симуляция «по образцу loop.mjs» → харнесс существует, зелёный (RS-baseline
  п.0.9) — способ реализуем ✔
- Markdown таблицы → строка «Итого» 4 ячейки, инсайты отдельным абзацем ✔

СОМНЕНИЯ:
- «~130 макс» у инструментов выше суммы волн RS-04 (~105–122) — допустимо как
  потолок с запасом, но пересчитать при ратификации волны инструментов.
- Симуляция 14 дней G9 меряет очередь БЕЗ прогресса реальных пользователей
  (свежий пользователь, грейды скриптом) — lab ≠ field; для дефолта достаточно,
  но не считать её доказательством полевого onboarding-опыта.

РЕЗЮМЕ: Все блокеры и существенные раундов 1–2 закрыты и перепроверены по тексту:
анти-регрессия волны 1 сведена по всем 6 темам, G7 получил trace-to-original,
таблица объёма трассируется к источникам, G9 измерим симуляцией, траектория первой
сессии заказана design-у. Остались только мелочи (в backlog STATE.md) и три
сигнала — числовая планка первого вау, пересборка RS-04 до волны 2, цена дефолта
non-exec. ТЗ готово к гейту пользователя.
```

---
---

# Раунд 2 (история): ДОРАБОТАТЬ

Протокол анти-зацикливания: (1) прошлые блокеры перепроверены по содержимому, не по
handoff-у; (2) правки проверены на регрессии; (3) новые претензии — только к
изменённому содержимому и по правилам конвертации.

## Разбор

### Б1 (анти-регрессия closures) — ЗАКРЫТ ✔

brief.md:40: волна 1 = разделы 1,2,7,17,18 **+ урок(и) «замыкания/захват переменных»
из раздела №4** = ~41 урок; формулировка теперь ссылается на матрицу RS-03 §4 и
называет закрытие closures явно. Перепроверено по всем 6 темам матрицы:
async-await→Р2 ✔, boxing→Р1 (урок 2, в волне) ✔, closures→Р4.3 (теперь в волне) ✔,
gc→Р7 ✔, hashtable→Р17.3 ✔, value-vs-reference→Р1 ✔. Счёт: 10+9+10+7+4+1 = 41 ✔.

### Б2 (Гудхарт G8: живой URL ≠ верный контент) — ЗАКРЫТ ✔

brief.md:75: G7 переименован в «Плотность и точность», способ включает
trace-to-original: «для каждого урока выборки — fetch источника и сверка ≥3 ключевых
утверждений с его контентом», порог «0 утверждений, не подтверждённых источником».
G8 (стр. 76) явно делегирует содержательную сверку G7-аудиту. Риск №2 пробы закрыт
измеримым способом. ✔

### С1 (таблица объёма + пометка RS-04) — ЧАСТИЧНО: новая ошибка арифметики ✘

Форма выполнена: таблица «источник → мин/макс → волны» (стр. 9–15) есть, пометка
«оценка „C# 15–20" в RS-04 УСТАРЕЛА, истина — RS-03 v2» есть. Но числа книжных строк
не трассируются к источникам — **Кормен посчитан в строке «читаемые»**:

| Строка таблицы ТЗ | Заявлено | По RS-02a/b | Расхождение |
|---|---|---|---|
| «Книги читаемые, 16 шт.» | ~280–380 | (190–260 − Кормен 40–60) + (90–120) = **~238–317** | завышено на ~40–60 (ровно ёмкость Кормена) |
| «Книги после OCR, 3 шт.» | ~45–60 | Кормен 40–60 + Фаулер 18–25 + EIP 10–14 = **~68–99** | занижено на ~25–40 |

Происхождение видно: «280–380» — сумма раунда 1 (190–260 + 90–120), где 190–260 —
это ВСЕ 10 книг части 1, включая нечитаемого Кормена (RS-02a §8); при расщеплении
на «читаемые/OCR» Кормен остался в первой строке и не доехал до второй. Итог
«~490–710» внутренне сходится со строками, но обе книжные строки противоречат
источникам. Нарушено правило доказательств + суть условия R-02 №1 (непротиворечивый
якорь объёма). Фикс — одна строка: читаемые ~240–320, OCR ~70–100, итог ~480–690.

### С2 (инкрементальная верификация) — ЗАКРЫТ ✔

brief.md:38: в P0-инфраструктуру добавлена «инкрементальная верификация (verify
только изменённых уроков …; полный прогон — на границе майлстоуна)» с обоснованием
через rate-limit 60 POST/мин. Соответствует пререквизитам RS-00. ✔

### С3 (G9 без числа) — ЗАКРЫТ ✔

brief.md:77: «≤10 новых карт/день (дефолт, конфигурируемо); контр-метрика: due
любого дня ≤25 карт». Порог до работ, дефолт ратифицируем на гейте. ✔
Оговорка (не блокирует): способ измерения «due ЛЮБОГО дня ≤25» не задан — GET
/api/due свежему пользователю меряет день 1; для дня N нужна симуляция времени
или аналитическая проверка в design (см. МЕЛОЧЬ ниже).

### Регрессии от правок

- brief.md:15: строка «**Итого**» таблицы склеена с абзацем «Ключевые инсайты…»
  в одну табличную строку — битый markdown, «Итого» имеет 5 ячеек при 4 колонках.
- brief.md:126: открытый вопрос 3 остался со старым дефолтом «40 уроков C# core» —
  противоречит новой волне «~41 + урок раздела 4» (стр. 40). Дефолт, который
  пользователь ратифицирует на гейте, не должен расходиться со скоупом.

### Конвертация по правилу релизного веса (review-verdict.md, анти-зацикливание п.4)

Пункт «ЧТО ПЛОХО» об опыте из раунда 1 — «траектория потребления волны 1
деконструирована только в точке смерти онбординга; порядок прохождения 40+ уроков,
состав первой сессии и место первого вау не заданы даже как требование к design» —
содержимое ТЗ по этому месту не менялось, пункт повторяется во втором вердикте
подряд → АВТОМАТИЧЕСКИ конвертируется в СУЩЕСТВЕННОЕ и эскалируется оркестратору
как кандидат в фикс-фичу. Фикс дешёвый: одно требование в ТЗ — «design.md обязан
задать порядок прохождения волны 1 (prereq-цепочки нового трека, состав первой
сессии, первый машинный вау ≤ N минут)».

## Вердикт (раунд 2)

```
ВЕРДИКТ: ДОРАБОТАТЬ

ПРОБЛЕМЫ:
- [СУЩЕСТВЕННОЕ] brief.md:13-14 (таблица объёма): книжные строки не трассируются к
  RS-02a/b — Кормен (40–60 уроков) посчитан в «читаемых 16» вместо OCR-строки:
  «читаемые ~280–380» против ~238–317 по источникам, «OCR ~45–60» против ~68–99 —
  нарушено правило доказательств и суть условия R-02 №1 — фикс: читаемые ~240–320,
  OCR ~70–100, итого ~480–690.
- [СУЩЕСТВЕННОЕ] (конвертация «ЧТО ПЛОХО»-опыта, 2-й вердикт подряд, содержимое не
  менялось): траектория потребления волны 1 за пределами дня-1 (G9) не
  деконструирована и не заказана design-у — порядок прохождения, первая сессия,
  первый вау — фикс: одно требование к design.md в ТЗ (prereq-цепочки, состав первой
  сессии, «первая машинная панель ≤ N минут»).
- [МЕЛОЧЬ] brief.md:15: строка «Итого» склеена с «Ключевые инсайты» — битая таблица
  (5 ячеек при 4 колонках) — фикс: вынести инсайты отдельным абзацем.
- [МЕЛОЧЬ] brief.md:126: открытый вопрос 3 — старый дефолт «40 уроков» противоречит
  волне «~41» (стр. 40) — фикс: синхронизировать.
- [МЕЛОЧЬ] brief.md:77 (G9): способ измерения контр-метрики «due любого дня ≤25»
  не задан (GET /api/due меряет день 1) — фикс: заказать design-у симуляцию дней
  или аналитическую проверку.
- [МЕЛОЧЬ, backlog раунда 1 — без изменений] G4-lint ловит только литеральный expect;
  правило случайной выборки G7-аудита (seed у evaluator-а); переопределение порога
  G1 после code-splitting.

ЧТО ПЛОХО:
- Опыт: пункт о траектории волны 1 конвертирован в СУЩЕСТВЕННОЕ (см. выше) — от
  референса boxing.ts (вау на первом уроке) ТЗ по-прежнему отделяет отсутствие
  спроектированной первой сессии.
- P1-инструменты всё ещё стоят на RS-04 без v2 (конвейерные «7 уроков» на
  инструмент, слой практиков не пробит) — реестр надо пересобрать до ратификации
  волны инструментов, иначе design унаследует картон.
- Конфликт ставки вау с дефолтом non-exec=MCQ не оценён в цене: у ~2/3 корпуса
  ставочная фича «exec-карточки с реальным stdout» не работает; ТЗ фиксирует вилку,
  но не называет, что теряет потребитель при дефолте.

ПРОВЕРКА ДОКАЗАТЕЛЬСТВ:
- «~41 урок волны 1» → 10+9+10+7+4+1 = 41 ✔
- «каждая из 6 тем имеет замену в волне 1» → перепроверено по матрице RS-03 §4,
  теперь верно (closures → Р4.3 в составе волны) ✔
- «книги читаемые ~280–380» → ОПРОВЕРГНУТО: по RS-02a/b ~238–317 (Кормен в чужой
  строке) ✘
- «итого ~490–710; ~60–100 майлстоунов» → внутренне сходится (493–711; при
  6–10/майлстоун = 49–118), но наследует ошибку книжных строк ✘/✔
- G7 trace-to-original, G9 ≤10/день, инкрементальная верификация в P0 → присутствуют
  в тексте, измеримы ✔

СОМНЕНИЯ:
- «~130 макс» у инструментов чуть выше суммы волн RS-04 (~105–122); допустимо как
  потолок с запасом, но на гейте волны инструментов число пересчитать.

РЕЗЮМЕ: Оба блокера раунда 1 закрыты честно и перепроверены (анти-регрессия по всем
6 темам; trace-to-original как измеримый порог G7). Возврат точечный: правка таблицы
объёма внесла новую ошибку арифметики (Кормен посчитан читаемым), плюс повторившийся
опыт-пункт о первой сессии волны 1 конвертирован в СУЩЕСТВЕННОЕ по правилу релизного
веса. Оба фикса — по одной-две строки; после них ТЗ готово к гейту.
```

---
---

# Раунд 1 (история): ДОРАБОТАТЬ

Порядок по протоколу: сначала построчный разбор, вердикт — последним, из разбора.

## 1. Построчный разбор по пунктам проверки

### 1.1 Цель, скоуп, планка

- **Цель** (стр. 3–4): сформулирована исходом («существует корпус… каждая волна
  проходит все харнессы зелёными и аудит плотности»), не действием. Измерима через
  G1–G9. ✔
- **Планка «ГОТОВЫЙ ПРОДУКТ на уровне каждой волны»** — легальна и честна: волна =
  выкатываемый прирост, DONE-WAVE-N поддержан конвейером; полный корпус объявлен
  дорожной картой, а не обещанием. Это правильный ответ на разрыв «520–650 уроков
  vs 6–10/майлстоун» из пробы. ✔
- **P0 тестируем**: инфраструктура, миграция удаления, волна 1 — каждый пункт
  проверяется гейтом (G5, G6, G9, G1). ✔ — с одним фатальным исключением, см. Б1.
- **P1/P2** — ратифицируются волнами, реестр покрытия обещан в features.md. ✔

### 1.2 Проверка утверждений ТЗ против источников (арифметика и трассировка)

| Утверждение ТЗ | Проверка | Результат |
|---|---|---|
| «~40 уроков» волны 1 (разделы 1,2,7,17,18) | RS-03 §3: 10+9+10+7+4 = 40 | ✔ сходится |
| «Ёмкость ≈ 520–650» | 141 + (100–130) + (280–380) = 521–651 | ✔ сходится |
| «книги ~280–380» | RS-02a ~190–260 + RS-02b ~90–120 (читаемые) | ✔ сходится |
| «16 читаемы; 3 скана + 1 djvu» | RS-02a (Кормен) + RS-02b (Фаулер, EIP, Седжвик) = 4 из 20 | ✔ сходится |
| «6–10 exec-уроков/майлстоун» | probe REPORT (оценка по факту сессии, не инструментирована — так и помечено) | ✔ с оговоркой |
| Базлайн-таблица (59 карт, 65/65, 571.88 KB, 316 сцен) | RS-baseline v2, числа из исполненных команд | ✔ сходится |
| «non-exec = MCQ легально по types.ts» | probe REPORT + types.ts:90 (`expect: ""` → MCQ-fallback) | ✔ |
| **«закрывают все 6 удалённых тем — анти-регрессия»** | RS-03 §4: closures → **Раздел 4, урок 3** («Лямбды и замыкания»). Раздела 4 в волне 1 НЕТ | ✘ **ОПРОВЕРГНУТО** |

**Разбор опровержения (Б1).** Матрица анти-регрессии RS-03 §4 отображает удаляемый
урок `closures` в раздел 4 «Делегаты и события» (урок 3 «Лямбды и замыкания: capture
семантика, компиляторные классы»). Волна 1 = разделы 1, 2, 7, 17, 18 — раздел 4 не
входит. Значит: P0 удаляет `closures.ts` (+сид, +карточку из FSRS), а замещающий урок
приходит только в волне 2+ (P1 «остальные core-разделы»). Тема замыканий исчезает из
корпуса на неопределённый срок — это ровно та регрессия, которую пункт P0 обещает
не допустить. Ошибка родом из RS-03 §8 (рекомендация повторяет ложное обоснование,
противореча собственному §4 — R-02 сверил арифметику 141, но не эту матрицу); ТЗ
перенесло её без проверки. Нарушено: правило доказательств (утверждение опровергается
собственным источником) + G5-принцип анти-регрессии самого ТЗ.

### 1.3 Ворота качества (метрика + способ + порог; Гудхарт-тест на каждый)

| Гейт | Метрика/способ/порог | Гудхарт-тест: как пройти, не дав ценности | Закрыто? |
|---|---|---|---|
| G1 | ✔ полный | держать новые уроки lazy, ядро не раздувать — это и есть цель | ✔ (но см. М3: порог «базлайн +10%» определён на до-сплитовом бандле) |
| G2 | ✔ полный | харнессы гоняют только зарегистрированные уроки → регистрация обязательна через G6 (index.ts+сид) | ✔ |
| G3 | ✔ полный | — | ✔ |
| G4 | ✔ полный | (а) `echo` ответа — закрыт lint-ом; (б) run **вычисляет** expect нетривиально (`Console.Write("4"+"2")`) — lint по подстроке не ловит | частично (М1) |
| G5 | ✔ полный | — | ✔ |
| G6 | ✔ полный | сжать «план волны» задним числом → план морозится в features.md на гейте; контр-метрика G7 | ✔ |
| G7 | ✔ полный | скрипт «≥90% динамических» проходится микродвижением 1 узла — закрыто аудитом evaluator-а 20%; НО выборку может подбирать builder | частично (М2) |
| G8 | **дыра** | живой URL + выдуманное утверждение/цитата проходят гейт: fetch проверяет ТОЛЬКО что URL отвечает, не что контент подтверждает урок | ✘ **Б2** |
| G9 | порога нет («фиксируется в фазе 3») | design может поставить порог = ∞; контр-метрики онбординга нет | ✘ (С3) |

**Разбор Б2.** Проба назвала невалидированные цитаты риском деградации №2: «поле
`sources[]` заполняется правдоподобно без fetch — выглядит идентично верифицированному.
Отличить можно только G2-аудитом». ТЗ закрыло риски №1 (lint эха), №3 (viz-fit через
регистрацию), №4 (аудит плотности) — а №2 не закрыт: G8 меряет живость URL, G7-аудит
меряет плотность, соответствие содержимого урока источнику не меряет НИКТО. Для
продукта, чья ставка вау — «реально снятое число» и провенанс каждого утверждения,
это незакрытый способ накрутки ключевого гейта = БЛОКЕР (принцип 4: тест Гудхарта).
Фикс дешёвый: в аудит выборки G7 (те же ≥20% уроков) добавить обязанность evaluator-а
сверить утверждения/цитаты сегментов с fetch-контентом sources[] (trace-to-original),
порог «0 неподтверждённых утверждений в выборке».

**Базовые гейты класса не пропущены**: якорь объёма с числами из RS есть (520–650;
волна 1 = 40 ≈ 7.7% ёмкости), измерительная обвязка строится как фичи (lint, лимит,
скрипт G7), improvement-базлайн заморожен таблицей из RS-baseline v2, контр-метрики
(guardrails) присутствуют парной таблицей. ✔

### 1.4 Ставка вау

Заполнена; ось ЕДИНСТВЕННАЯ и конкретная («уровень ниже абстракции источника»,
спроецирована на три домена, но это одна ось); ставочные фичи названы (машинная
панель + exec-карточки); риск скуки с контрходом. ✔ Слабое место — конфликт с
дефолтом MCQ для non-exec, см. «ЧТО ПЛОХО» п.3.

### 1.5 Паспорт вкуса

Конкретен в словаре класса: референсы — собственные артефакты (boxing.ts 7 сегментов /
IL-панель; py-generators.ts с реальным замером 38.4 MB vs 200 B), деконструкция голоса
(«сегмент = механизм в движении»), frozen-токены, жанровый анти-слоп (0 chip+подпись,
0 буллет-конспектов, запрет суперлативов, сверка фактов <2018), signature-элемент
назван и совпадает со ставкой вау. ✔ Соответствует принципу 11.

### 1.6 Условия R-02 (обязательные, «иначе вернутся блокерами»)

| Условие | Статус в ТЗ | Оценка |
|---|---|---|
| 1. Сводная таблица объёма (источник → min/max → волна); строку RS-04 «C# 15–20» пометить устаревшей | Числа по источникам — прозой в «Контексте»; формы «таблица … → волна» нет; пометка RS-04 не сделана нигде | ЧАСТИЧНО (С1) |
| 2. Механизм non-exec verify + lint «run не содержит ответ» | P0: «механизм verify ЛИБО явное решение non-exec=MCQ в design» + ADR-допущение в рисках; lint в P0 и G4 | ✔ (решение с зафиксированным дефолтом — легально) |
| 3. Лимит новых карт/день | P0 (инфраструктура) + G9 | ✔ (но порог отложен — С3) |
| 4. Харнессы run.mjs/new-lessons.mjs в скоупе удаления | P0, дословно | ✔ |
| 5. Расширение LessonLang | P0, дословно | ✔ |

### 1.7 Субъективные слова запроса

Таблица есть: «куча уроков» → G6+G7 (прокси + контр-метрика), «все книги» → реестр
покрытия с явными срезами (нефальсифицируемое «всё» честно заменено таблицей — и
продублировано в «Чувствительности» как вопрос гейта). «Глубоких» → G7 + паспорт.
✔ Принцип 3 выполнен.

### 1.8 Полномочия и деструктив

- Удаление 6 уроков: преавторизация явная, с точным составом (TS+index+сид+харнессы),
  «после „Утверждаю"» — деструктив покрыт. ✔
- Чистка сирот — только dev-SQLite; прод-БД, деплой, push, покупки — запрещены. Лишнего
  не выдано. ✔
- Копирайт книг: дефолт «идеи своими словами», дословно — только открытые доки; вопрос
  на гейте. Совпадает с RS-00 (юрист/пессимист-2). ✔

### 1.9 Прочее (жанровая полнота, траектория, известные провалы)

- Язык продукта + глоссарий — есть (урок провала CodeFarm-2 применён). ✔
- py-трек неприкосновенен + G5 — есть (RS-00 угол 10). ✔
- Из инфраструктурных пререквизитов RS-00 («code-splitting, лимит карт, навигация,
  **инкрементальная верификация** — ДО волны книг») ТЗ взяло три из четырёх:
  инкрементальной верификации нет ни в P0, ни в P1. RS-baseline п.6.2 предупреждает:
  rate-limiter 60 POST/мин → verify-прогоны растут линейно с корпусом; на сотнях
  уроков полный прогон харнессов станет часами, и G2 начнут гонять «не целиком» —
  тихая эрозия гейта. → С2.
- Траектория потребления волны 1 деконструирована частично: смерть онбординга (due
  дня 1) поймана и гейтится (G9), навигация в P0; но порядок прохождения нового трека
  (prereq-цепочки 40 уроков, «где первый вау») не упомянут даже как требование к
  design. → «ЧТО ПЛОХО» п.1.

## 2. Cut-list (цена среза — в единицах ущерба потребителя: темы без ответа,
минуты онбординга, риск устаревших фактов). Решение по срезам — за пользователем на гейте.

| # | Срез | Экономия | Цена для потребителя |
|---|---|---|---|
| 1 | Optional-разделы C# (33 урока, хвост P1) | ~3–5 майлстоунов | сеньор без expression trees / unsafe / строк-вглубь: ~23% тем реестра остаются «вопросом без ответа»; для волн 1–2 ущерб низкий |
| 2 | ADVANCED-инструменты (Kafka, K8s, nginx, gRPC, SignalR ≈ 26 уроков) | ~3–4 майлстоуна | нет event-streaming и оркестрации — заметная дыра в профиле «сеньор-бэкендер», но CORE-инструменты закрывают ежедневную работу |
| 3 | Книги-дубли как самостоятельные треки (Грокаем, Дэвис, Тюкачев, Рихтер-трек) | ~50–70 уроков | ≈ 0: концепты покрыты C#-треком/алго-разделом; книги остаются донорами перспектив (так и предлагает RS-02a/b) |
| 4 | P2 OCR-волна (Кормен, Фаулер, EIP ≈ 40–55 уроков) | OCR-работа + ~5 майлстоунов | алго-глубина и enterprise-паттерны выпадают; «все книги» выполняется реестром, но 3 строки навсегда «срезано с причиной» |
| 5 | Разделы 19–20 C# (STJ + Channels, 8 уроков) из волн 2 | ~1 майлстоун | две частые сеньор-темы без покрытия; анти-регрессии не задевает — переносимо в хвост |
| 6 | Сжатие волны 1: разделы 2 и 7 с 9+10 до ~6+6 уроков | ~1 майлстоун | темы удалённых уроков всё ещё покрыты (анти-регрессия жива), но глубина async/GC ниже планки py-эталона — допустимо только как аварийный ход после бюджетного чекпойнта M1 |
| 7 | Сегмент «книга vs рантайм 2026» | ~10–15% цены книжного урока | устаревшие факты книг 2011–2018 доезжают до пользователя как истина; бьёт ось вау и G8 — НЕ РЕКОМЕНДУЮ |
| 8 | Лимит карт/день, code-splitting, миграция харнессов, навигация | дни работы | НЕ РЕЗАТЬ: due дня 1 = сотни карт (смерть онбординга), красный verify, свалка на home — инфраструктурный P0 и есть то, что отличает корпус от свалки |

## 3. Вердикт (раунд 1)

```
ВЕРДИКТ: ДОРАБОТАТЬ

ПРОБЛЕМЫ:
- [БЛОКЕР] brief.md:32 (P0, волна 1): утверждение «закрывают все 6 удалённых тем —
  анти-регрессия» ложно: по матрице RS-03 §4 тема closures замещается разделом 4
  («Лямбды и замыкания», урок 3), которого в волне 1 (разделы 1,2,7,17,18) НЕТ;
  удаление closures.ts в P0 оставляет тему без замены до волны 2+ — нарушено правило
  доказательств и собственный P0-критерий анти-регрессии (ошибка унаследована из
  противоречия RS-03 §8 ↔ §4) — фикс: включить в волну 1 раздел 4 (либо минимум урок
  Р4.3, волна = 41–47 уроков) ИЛИ отложить удаление closures до волны с разделом 4;
  матрицу §4 перепроверить по всем 6 темам в features.md.
- [БЛОКЕР] brief.md:68 (G8): гейт провенанса Гудхартится — «URL живой (fetch)»
  проходится выдуманным утверждением с реальной ссылкой; риск №2 из probe REPORT
  («невалидированные цитаты выглядят идентично верифицированным») не закрыт ни одним
  гейтом (G7-аудит меряет только плотность) — нарушен принцип «тест Гудхарта на
  каждый гейт» — фикс: в аудит выборки ≥20% (G7) добавить сверку утверждений/цитат
  с fetch-контентом sources[] (trace-to-original), порог «0 неподтверждённых
  утверждений в выборке».
- [СУЩЕСТВЕННОЕ] brief.md:7 (Контекст): условие R-02 №1 выполнено частично — якорь
  объёма дан прозой, формы «источник → уроки min/max → волна» нет; строка RS-04
  «C# трек 15–20 уроков» нигде не помечена устаревшей — фикс: таблица объёма в ТЗ
  (или features.md с ссылкой из ТЗ) + пометка в RS-04/STATE.
- [СУЩЕСТВЕННОЕ] brief.md:30 (P0-инфраструктура): из четырёх пререквизитов массового
  авторинга (RS-00 синтез) отсутствует инкрементальная верификация; при rate-limiter
  60 POST/мин (RS-baseline §6.2) полный verify на сотнях уроков деградирует до часов
  и G2 начнут гонять частично — фикс: внести «инкрементальный/шардированный прогон
  харнессов» минимум в P1 до волны книг + контр-метрику «время полного verify».
- [СУЩЕСТВЕННОЕ] brief.md:69 (G9): гейт без числа («порог из design.md») и без
  контр-метрики — формально «порог до работ» соблюдён (фиксация в фазе 3 до сборки),
  но сейчас гейт неизмерим и design может поставить любой порог — фикс: задать в ТЗ
  дефолт-вилку (например, 10–20 новых карт/день — типичный диапазон SRS-практики)
  как отправную точку ратификации, design волен уточнить с обоснованием.
- [МЕЛОЧЬ] brief.md:64 (G4): lint «run не содержит expect» ловит только литеральное
  вхождение; вычисленный ответ (Console.Write("4"+"2") при expect "42") пройдёт —
  фикс: включить проверку «run понятийно решает задачу, а не печатает константу»
  в аудит выборки.
- [МЕЛОЧЬ] brief.md:67 (G7): выборка «≥20% уроков» без правила отбора — builder может
  подсунуть лучшие — фикс: случайная выборка, seed фиксирует evaluator.
- [МЕЛОЧЬ] brief.md:61 (G1): порог «начальный чанк ≤ базлайн +10%» определён на
  до-сплитовом бандле (571.88 KB = движок+экраны+уроки); после code-splitting порог
  разумно переопределить как «чанк без данных уроков ≤ X» — фикс: уточнить в design.

ЧТО ПЛОХО:
- Опыт потребителя: траектория волны 1 деконструирована только в точке смерти
  онбординга (G9). Порядок прохождения 40 новых уроков (prereq-цепочки, состав
  «первой сессии», где случается первый вау «машинная панель с реальным числом» —
  у референса boxing.ts это первый же урок) не задан даже как требование к design —
  риск, что технически зелёная волна встретит пользователя случайным порядком тем.
- P1-трек инструментов стоит на самом слабом отчёте корпуса: RS-04 без v2
  (конвейерные «7 уроков» на каждый инструмент, слой практиков не пробит, NUnit
  «v5+» опровергнут ещё в R-02) — дизайн волны инструментов унаследует картонный
  реестр, если не пересобрать его до ратификации волны.
- Ставка вау внутренне конфликтует с дефолтом non-exec=MCQ: «exec-карточки с реальным
  stdout» — ставочная фича, но у ~2/3 целевого корпуса (книги-советы, инструменты
  без харнесса) её не будет; ось «уровень ниже абстракции» рискует выродиться в
  C#-only, а книги — в анимированные MCQ-конспекты. ТЗ честно фиксирует вилку, но
  цену дефолта (потеря generation-петли для 2/3 корпуса) не называет.
- «Аудит плотности» в цели опирается на evaluator-суждение «0 конспектов»; определение
  конспекта живёт только в паспорте — для батч-аудита сотен уроков понадобится
  замороженная бинарная рубрика, иначе планка поплывёт между волнами.

ПРОВЕРКА ДОКАЗАТЕЛЬСТВ:
- «~40 уроков волны 1» → пересчитано по RS-03 §3: 10+9+10+7+4 = 40 ✔
- «Ёмкость 520–650» → 141 + (100–130) + (280–380) = 521–651 ✔
- «закрывают все 6 удалённых тем» → ОПРОВЕРГНУТО матрицей RS-03 §4 (closures → раздел 4) ✘
- Базлайн-таблица → совпадает с RS-baseline v2 (исполненные команды) ✔
- «6–10 уроков/майлстоун» → probe REPORT, оценка честно помечена неинструментированной ✔
- «16 читаемы; 3 скана + 1 djvu» → RS-02a/b: Кормен, Фаулер, EIP, Седжвик = 4 из 20 ✔
- «non-exec = MCQ легально» → types.ts:90 + AUTHORING-AI §9 через probe REPORT ✔

СОМНЕНИЯ:
- «Ротация builder-а по волнам» как контрход конвейерной одинаковости — декларация
  без механизма в скоупе; не проблема ТЗ, но design должен сказать, чем это меряется.
- Бюджетный чекпойнт после M1 — правильный, но критерий «оценка не подтвердилась»
  не имеет порога (во сколько раз дороже = стоп?); предлагаю зафиксировать на гейте.

РЕЗЮМЕ: ТЗ сильное — планка волн честная, ставка вау единственная и конкретная,
паспорт вкуса в словаре класса, условия R-02 в основном выполнены, полномочия
покрывают деструктив без излишка. Возврат из-за двух дыр: ложная анти-регрессия
волны 1 (тема closures удаляется без замены — противоречие с собственным источником
RS-03 §4) и Гудхартящийся G8 (живой URL не доказывает верность контента — риск,
который проба назвала главным после эха). Оба фикса локальны и не требуют пересборки
ТЗ.
```
