# M3 step 0 — remove the last hardcoded retired-lesson id from home.ts. command + output
date: 2026-07-18

## Finding (orchestrator, M2 verify): home.ts:371 first-run CTA hardcoded the DELETED id
  return (rows.find((r) => r.lesson.id.includes("value-vs-reference")) ?? rows[0]).lesson.id;

## Fix: first-run CTA = first UNCOMPLETED lesson in curriculum order (rows follow the registry
## prereq/curriculum order) — no hardcoded id.
  return (rows.find((r) => !r.completed) ?? rows[0]).lesson.id;

## Proof
$ grep -rn "value-vs-reference" app/src
(no output)   grep exit 1 = 0 matches = PASS
$ npm run build -> tsc clean, built OK
run.mjs   ALL GREEN (home + first-run CTA)
shell.mjs ALL GREEN (Progress/Profile/home, axe 0 serious/critical)
